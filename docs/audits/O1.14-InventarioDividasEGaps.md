# O1.14 — Inventário Consolidado de Dívidas Técnicas e GAPs Funcionais

> Produzido na Work Order O1.14 (Blueprint de Banco e Validação Funcional Final), a partir de leitura de `.ai/BACKLOG.md`, das seções finais de todos os Work Orders `completed/` de O1.1 a O1.13.5, e da auditoria técnica direta do modelo EF Core/RBAC/Multi-BU realizada nesta sprint. Alimenta a etapa posterior de auditoria dos 41 entregáveis e Gate Final da Onda 1 — **não resolve nem prioriza essas dívidas**, apenas as consolida e classifica.
>
> **Atualização — Gate Final da Onda 1 (12/08/2026):** este inventário foi revisitado e as dívidas priorizadas/quitadas conforme a Regra dos 100% e a Seção 9 do Gate. Ver coluna "Status" de cada linha (marcadas `[GATE]`) para o resultado desta etapa. Resumo: DEB-03 corrigida com implementação real e testes de regressão; DEB-13 investigada e reclassificada SUPERADA (não reproduz); DEB-15/M2 corrigida (transação atômica real); DEB-16 (propósito de criptografia) corrigida; demais dívidas avaliadas e mantidas por decisão objetiva de custo/risco/escopo, listada individualmente.

## Legenda de status

- **RESOLVIDA** — quitada por uma Work Order posterior, com evidência objetiva.
- **ABERTA** — permanece sem correção até o fechamento desta sprint.
- **SUPERADA** — o contexto que originou a dívida deixou de existir (ex.: código removido/substituído).
- **NÃO APLICÁVEL** — não representa uma dívida real (ex.: escopo intencionalmente futuro).

## Dívidas técnicas

| ID | Origem | Descrição | Severidade | Superfície | Status |
|---|---|---|---|---|---|
| DEB-01 | O1.3.1/O1.4.1/O1.5/O1.6/O1.11/O1.12 | Catálogo definitivo de Perfis/Permissões **de negócio** (além do catálogo técnico de 19 permissões já implementado) permanece pendência de decisão de produto | LOW (produto, não código) | RBAC | **MANTER [GATE]** — decisão indispensável do Product Owner (conteúdo de negócio), não corrigível tecnicamente |
| DEB-02 | Desde B1/B2 (Fornecedor) | `Fornecedor.BusinessUnit` é `string` livre, sem `UnidadeNegocioId`/FK — único domínio administrativo cujo isolamento multi-BU não usa o modelo formal introduzido em O1.4.2 | MEDIUM | Fornecedor / Multi-BU | **MANTER [GATE]** — avaliada no Gate Final: hoje o sistema opera mono-BU real (só "Grupo Soma" existe), `BusinessUnit` é valor arbitrário do cliente (não vem de config real), e adicionar `UnidadeNegocioId` físico tocaria ~20 arquivos de produção/teste. Risco não explorável hoje (mono-BU); custo desproporcional a este Gate. Registrada como GAP arquitetural a resolver antes de uma segunda Unidade de Negócio real entrar em operação — não bloqueia a Onda 2. |
| DEB-03 | O1.14 (nova, esta auditoria) | `ObterSincronizacaoFornecedorUseCase`/`SincronizacaoFornecedorMonitorRepository.ObterPorIdComErrosAsync` busca por `Id` sem filtrar por BU | MEDIUM | Monitoramento operacional (Fornecedores) | **RESOLVIDA [GATE]** — `UnidadeNegocioId` real adicionado a `SincronizacaoFornecedor` (migration `AddUnidadeNegocioIdToSincronizacaoFornecedorDEB03`), capturado da sessão (nunca do cliente) na criação, e usado para filtrar `Listar`/`ObterDetalhe` no controller e no repositório. 2 testes de regressão cross-BU adicionados (BU A não vê/lê execução da BU B). |
| DEB-04 | O1.11/O1.14 (confirmada nesta auditoria) | Tabelas Multi-BU (`IdentityProviders`, `ConfiguracoesErp`, `ConfiguracoesNotificacao`, `FeatureFlagsUnidadesNegocio`) têm coluna `UnidadeNegocioId` mas **sem FK física** para `UnidadesNegocio` — isolamento depende só da aplicação | LOW | Multi-BU | **MANTER [GATE]** — baixa severidade, isolamento já é garantido na camada de aplicação (RBAC + resolução de sessão); adicionar FK física é hardening de baixo risco, mas não bloqueante; candidata a próxima sprint de hardening |
| DEB-05 | O1.12 (confirmada nesta auditoria) | `AlcadaAprovacao`/`RegraWorkflow`/`RegraOrcamentaria` referenciam `Usuario`/`Perfil`/`CentroCustoMetadado` por Guid solto, sem FK física — "referências fracas validadas na aplicação" (comentário no próprio código) | LOW | Workflow/Alçada/Orçamento | **MANTER [GATE]** — decisão arquitetural deliberada e documentada no próprio código, não erro |
| DEB-06 | O1.13.5 | Duplicação de helper de conexão `SOMA_DESENV` entre `SomaFilialReader`, `SomaCentroCustoReader` e `LinxSchemaDiscoveryReader` | LOW | ERP readers | **MANTER [GATE]** — duplicação de baixo custo, sem risco funcional; extração para helper compartilhado é melhoria de manutenibilidade, não bloqueante |
| DEB-07 | O1.2.1/O1.3.4/O1.3.5 | Divergência de porta entre `.env.example` (5262) e proxy Vite — identificada desde O1.2.1, corrigida incidentalmente durante O1.4.2 no proxy real, mas nunca formalizada como correção dedicada | LOW | Frontend/DX | **SUPERADA** (correção incidental confirmada no relatório da O1.4.2) |
| DEB-08 | O1.3.3/O1.3.4/O1.3.5 | Duplicação de padrão página/hook/tabela/mock entre módulos administrativos do frontend — candidata a extração para `administration/shared` | LOW | Frontend | **MANTER [GATE]** — refatoração de manutenibilidade sem risco funcional; extrair agora seria gold-plating fora do objetivo do Gate |
| DEB-09 | O1.4.2 | `EmailAuditHasher` sem salt/HMAC; sincronização de migrations do banco compartilhado `MaisComprasConnection` dessincronizada (pendência de infraestrutura de dados, não de código) | LOW/MEDIUM | Autenticação | **MANTER [GATE]** — a parte de infraestrutura (dessincronização do banco compartilhado) é pendência operacional externa a este Gate; o hardening de `EmailAuditHasher` é candidato a hardening de Homologação (junto com DEB-10), não código quebrado hoje |
| DEB-10 | O1.4.3 (Security Validation) | MEDIUM 1 (`Cache-Control: no-store` ausente em `/bootstrap/*`) e MEDIUM 2 (Bootstrap Secret sem validação de entropia) — **bloqueiam explicitamente Homologação**, não Development | MEDIUM | Bootstrap/Segurança | **MANTER [GATE]** — já classificadas pelo próprio Security Gate como bloqueadoras apenas de Homologação, não da Onda 1/Development; ressalvas já aceitas formalmente pelo Product Owner |
| DEB-11 | O1.4.3 | 6 LOW + 5 INFORMATIONAL da Security Validation do Bootstrap (throttle de e-mail, fallback CORS, detecção de índice único por substring, revogação de sessão em falha, rate limit por IP, `UseForwardedHeaders()` ausente) | LOW/INFO | Bootstrap/Segurança | **MANTER [GATE]** — já aceitas formalmente pelo Product Owner na O1.4.3, sem risco explorável relevante |
| DEB-12 | O1.5 | Ressalvas remanescentes aceitas: invariante anti-auto-bloqueio não serializada (M1), backfill por nome de Perfil (L1), sem auditoria RBAC (L2), sem rate limiting administrativo (L3); 4 Perfis de smoke test mantidos deliberadamente no banco de desenvolvimento | LOW | RBAC | **MANTER [GATE]** — ressalvas já aceitas formalmente pelo Product Owner na O1.5; dados de smoke test em DEV são decisão deliberada (ver Seção 18 do Gate) |
| DEB-13 | O1.5 | Drift de schema (`Invalid column name 'Cnpj'/'Nome'`) encontrado fora do escopo original da sprint | MEDIUM | Fornecedor (schema legado) | **SUPERADA [GATE]** — investigada nesta etapa: `FornecedorConfiguration.cs` mapeia `RazaoSocial`→`Nome`/`Cnpj_Cpf`→`Cnpj` intencionalmente (colunas físicas legadas preservadas por design); `dotnet ef migrations has-pending-model-changes` confirma modelo 100% sincronizado; nenhum SQL raw referencia os nomes antigos. Não reproduz no código atual — provável artefato de ambiente/sessão pontual já superado. |
| DEB-14 | O1.6 | Sem Security Validation dedicada para Usuários (M1); documentação de banco incompleta à época (L1, **resolvida por esta própria sprint O1.14**); usuário de teste remanescente no banco (I1) | LOW | Usuários | DEB-M1 **MANTER [GATE]** (candidata a próxima revisão de segurança consolidada); I1 **MANTER [GATE]** (dado de smoke test em DEV, decisão deliberada — Seção 18); L1 **RESOLVIDA** (O1.14) |
| DEB-15 | O1.7 | Ancoragem de `CentroCustoMetadado` sem transação compartilhada com o vínculo Usuário×Centro de Custo (M2); sem paginação, limite de 5000 linhas na leitura ERP (L3); duplicação entre readers ERP (L4, mesma raiz de DEB-06); catálogo ERP visível a todas as UNs sem filtro (I1, decisão de produto pendente) | LOW/MEDIUM | Filial/Centro de Custo | M2 **RESOLVIDA [GATE]** — `CentroCustoVinculoValidator` deixou de commitar isoladamente; ancoragem e vínculo agora persistem em um único `SaveChangesAsync` (mesmo padrão de `ConcluirBootstrapUseCase`), com tradução de índice único para `DuplicateRecordException` tratada; 6 novos testes de regressão. L3/L4/I1 **MANTER [GATE]** (L4 é a mesma dívida de DEB-06; I1 é decisão de produto pendente) |
| DEB-16 | O1.11 | Notificações em escopo mínimo, catálogo de eventos pendente de produto; propósito de criptografia compartilhado no `DataProtection` (LOW) | LOW | Multi-BU/Notificações | Propósito de criptografia **RESOLVIDA [GATE]** — `IdentityProviderSegredoProtector`/`ConfiguracaoErpSegredoProtector` agora usam propósitos `DataProtection` distintos e não intercambiáveis (teste de regressão prova que um não decifra o outro); catálogo de eventos de notificação **MANTER [GATE]** (decisão de produto, fora do alcance técnico) |
| DEB-17 | O1.13 | Ausência de testes de integração HTTP (`WebApplicationFactory`) cobrindo RBAC nos demais controllers administrativos (apenas Fornecedores tem essa cobertura, herdada de B2.1.3) | LOW | Testes | **MANTER [GATE]** — cobertura unitária de RBAC já existe (policies testadas), ampliar para testes HTTP completos em todos os 17 controllers é investimento de teste de médio porte, não corretivo de bug; candidata a próxima sprint de qualidade |
| DEB-18 | O1.13.5 | Evolução da busca textual/estruturada para embeddings/RAG é um ponto de extensão planejado, não implementado; ingestão dos +300 `obj_*.prg`/documentação Linx é fora de escopo; frontend administrativo de conhecimento não exigido | INFO | Agents Linx / AI Factory | **NÃO APLICÁVEL** (arquitetura-alvo declarada, não dívida) |

## GAPs funcionais (entregável incompleto, distinto de dívida técnica)

| ID | Descrição | Onde está hoje | Status |
|---|---|---|---|
| GAP-01 | Módulo de Pedido de Compra | Permissões `Pedido.Criar`/`Pedido.Aprovar`/`Pedido.Cancelar` pré-provisionadas no catálogo RBAC, sem controller/tela correspondente | Fora do escopo da Onda 1 (Onda 3/4) — **NÃO É GAP da Onda 1**, é escopo futuro já conhecido |
| GAP-02 | Especificação funcional/UX completa por tela dos artefatos #33/#34/#35 (`ComprasFuncional.md`/`ComprasUX.md`/`ComprasDataModel.md`) | Estrutura documental inicial existe; especificação completa por tela ainda pendente (nota já registrada em `docs/audits/Onda1-Reconciliacao-e-Plano-Execucao.md`, seção 3) | **MANTER [GATE]** — #33–#35 já estão classificados como Concluído (estrutura documental inicial, seu requisito formal registrado); especificação exaustiva por tela é atividade de Design Review, deliberadamente fora do escopo deste Gate (Seção 23) |
| GAP-03 | Sub-telas Multi-BU sem UX detalhado (Notificações, IdP, Feature Flags) | Backend/persistência real existe (O1.11); UX/especificação detalhada de produto ainda pendente para parte das sub-telas | **MANTER [GATE]** — mesmo motivo de GAP-02, refinamento de UX não funcional é escopo do Design Review consolidado pós-Onda 1 |
| GAP-04 | Motor de aprovação operacional real (Workflow/Alçada/Orçamento) | Apenas a fundação configurável (O1.12) existe; motor de execução/aprovação operacional é Onda 3/4 | Fora do escopo da Onda 1 — **NÃO É GAP da Onda 1** |

## Achados de segurança desta sprint (O1.14)

- **DEB-03** (exposição cross-BU em `SincronizacaoFornecedor` por Id) — classificado **MEDIUM**, não bloqueante, registrado para hardening.
- Nenhum achado **CRITICAL** ou **HIGH** identificado na auditoria realizada nesta sprint (escopo: modelo EF Core, RBAC/enforcement, isolamento Multi-BU, migrations, Agents Linx). Consistente com os relatórios de segurança já registrados nas sprints anteriores (O1.5, O1.13, O1.13.5 sem CRITICAL/HIGH).

## Observação de fechamento

Este inventário **não quita nenhuma dívida** — apenas consolida, elimina duplicidade documental entre Work Orders e classifica severidade/status para uso na próxima etapa formal (auditoria exaustiva dos 41 entregáveis, implementação de GAPs, pagamento de dívidas razoáveis, hardening, Gate Final da Onda 1). Nenhuma decisão de priorização foi tomada aqui.
