# PROJECT_STATE.md

> Estado canônico operacional do SOMA BlueprintOS / +COMPRAS. Atualizar ao concluir cada sprint.

> Política de autonomia dos agentes: [AI_AUTONOMY_POLICY.md](./AI_AUTONOMY_POLICY.md).

> Referência técnica consolidada: [ENGINEERING_BLUEPRINT.md](./ENGINEERING_BLUEPRINT.md).

> Fonte externa de descoberta: [COMPRAS_INDIRETAS_SOURCES.md](./sources/COMPRAS_INDIRETAS_SOURCES.md) (não é evidência de implementação).

## Atualização

- **Data:** 03/08/2026
- **Branch:** `feature/a13-procurement-vertical-slice`
- **Commit de referência:** `601d937` e `7bf3bf4` para a remoção do Docker e consolidação do ambiente local; `b08769f`, `3b6d54b` e `0240c35` para as entregas B2.1 e B2.1.1; `77861eb` para B2.1.2 estrutural; `5a6aab8`, `234906c` e `32c9971` para B2.2; hardening limite/ChangeTracker da B2.1.3 registrado na atualização anterior.
- **Validação desta atualização:** Sprint de infraestrutura "Remoção do Docker e Consolidação do Ambiente Local" concluída e auditada. `dotnet build backend/BlueprintOS.sln` aprovado, 0 erros e 0 avisos. `dotnet test backend/BlueprintOS.sln` aprovado: 286 testes (281 unitários + 5 integração), 0 falhas. `npm run build` do frontend (`tsc -b && vite build`) aprovado. Scripts de desenvolvimento local (`start-dev.sh`/`stop-dev.sh`/`health-check.sh`) validados. Docker, `Makefile`, `Dockerfile` e `docker-compose.yml` foram removidos do fluxo de desenvolvimento; o ambiente oficial passa a ser 100% local, sem containers (ver ADR-0018 atualizada em `.ai/DECISIONS.md`). Auditoria final não encontrou resíduo funcional, referência quebrada ou documento contraditório. Detalhes em `.ai/CURRENT_SPRINT.md` e `.ai/memory/completed_sprints.md`.

## Sistema de Work Orders

- **Estado:** Implementado em 30/07/2026.
- **Evidência:** [templates/README.md](./templates/README.md) e os sete templates padronizados para desenvolvimento, épicos, auditorias, refatorações, hotfixes, spikes e releases.
- **Uso:** os templates complementam, sem substituir, as Work Orders estratégicas em `.ai/work-orders/backlog/` (planejado/parcial) e `.ai/work-orders/completed/` (concluído) e a governança de [WORKFLOW.md](./WORKFLOW.md). Eles exigem leitura prévia de visão, workflow, estado do projeto e sprint atual.

## Evolução arquitetural do +Compras

- **Decisão aceita:** [ADR-0013](./DECISIONS.md) estabelece a evolução em dois momentos: plataforma operacional primeiro e inteligência progressiva sobre dados reais depois.
- **Princípio obrigatório:** toda operação crítica possui alternativa manual; IA acelera e orienta, mas não é pré-requisito para cadastrar ou selecionar fornecedor/item, criar pedido, enviá-lo ao ERP ou acompanhar a integração.
- **Portal:** a ADR-0017 definiu o Portal Operacional +Compras como navegação e identidade visual completas, com evolução funcional incremental por domínio. B2.2.4 concluiu a primeira vertical slice funcional em React com a tela `CadastroFornecedor`. A próxima frente formal é o Portal +Compras Frontend, a ser executada pelo Claude Code.
- **B2/B2.1/B2.1.1:** B2 permanece como estrutura inicial de descoberta e score (100/80/60/40). B2.1 concluiu sincronização bidirecional, regra temporal, inativação, auditoria e concorrência; B2.1.1 concluiu o mapeamento canônico ERP → +Compras.
- **B2.1.2 estrutural:** concluída conforme ADR-0016, com modelo fornecedor alinhado ao Linx: `Cnpj_Cpf`, `TipoPessoa`, separação de `RazaoSocial`/`NomeFantasia`, proteção do nome fantasia controlado pelo Linx, flags `Beneficiador`/`Licenciado`, domínios ERP estruturados, FKs opcionais e contrato frontend inicial.
- **B2.1.3 operacional:** concluída para endurecer o fluxo ERP SOMA → +Compras: leitura paginada/lotes, histórico de execução, erros parciais persistidos, logs estruturados, métricas de sincronização e retorno detalhado em `GET /api/fornecedores/sincronizar-erp`. Dois bugs de paginação encontrados pelo teste `Execute_Should_Process_Multiple_Batches_And_Calculate_Totals` foram corrigidos: parada prematura em lote parcial (`21f1a67`) e cálculo não determinístico do offset (`ca48dc3`). Em 02/08/2026, a validação real contra Docker/VPN/SQL Server corporativo encontrou e corrigiu mais três problemas: dependência Docker desnecessária que impedia a API de subir, `limite` tratado como tamanho de página em vez de teto total de fornecedores processados, e erro parcial de persistência que virava HTTP 500 por poluição do `ChangeTracker` do EF Core. Nenhuma regra de negócio foi alterada em nenhuma correção. `dotnet test backend/BlueprintOS.sln` executado com sucesso: 282 testes, 0 falhas.
- **B2.2:** concluída como Enriquecimento Inteligente de Fornecedor. O módulo de fornecedores possui cadastro, consulta CNPJ, enriquecimento, aprovação, rejeição, integração ERP e auditoria. A B2.2.1 foi concluída com contrato `ICnpjConsultaProvider`, retorno tipado e auditoria persistida. A B2.2.2 implementou `BrasilApiCnpjProvider` como adaptador gratuito BrasilAPI. A B2.2.3 concluiu comparação campo a campo, aprovação/rejeição, atualização seletiva, auditoria de decisões e proteção `NomeFantasia`/Linx. A B2.2.4 concluiu o portal funcional de cadastro, consulta, divergências e decisão humana.
- **Portal +Compras Frontend:** concluído tecnicamente no frontend (commit `8ee8f4e`, branch `feature/a13-procurement-vertical-slice`). Shell de navegação (AppShell) e rotas React Router criados; módulo Fornecedores integrado à API real (cadastro, consulta CNPJ, enriquecimento, aprovação/rejeição); demais módulos (Pedidos, Negociações, Indicadores, Agentes IA, Configurações) implementados como telas demonstrativas honestas, sem persistência simulada; Design System AZZAS 2154/GDT aplicado. Build frontend aprovado (`tsc` + `vite build`, 4/4 testes). Revisão manual de código confirmou os endpoints de fornecedores, a regra `Cnpj_Cpf` (`varchar(14)` alfanumérico), a proteção de `NomeFantasia` (só editável quando origem é ERP) e o alerta não bloqueante de situação cadastral `Baixada/Suspensa/Inapta`. Validação de backend (`dotnet build`/`dotnet test`) **não foi executada neste ciclo** por ausência do SDK .NET no ambiente de revisão; permanece pendente de execução local. Roteiro de demonstração em `docs/demo/PortalMaisComprasDemo.md`.

## Estratégia de LLM

- **Decisão aceita:** a [ADR-0014](./DECISIONS.md) determina `IAIProvider` e `IAIRuntime` como fronteira entre a aplicação e qualquer fornecedor de LLM.
- **Desenvolvimento:** Ollama local é o padrão arquitetural, com preferência por modelos de 3B a 4B parâmetros; seu adaptador ainda não foi implementado ou configurado por esta decisão documental.
- **Produção:** a plataforma corporativa de IA, definida pela Infraestrutura/Arquitetura Corporativa, será consumida por adaptador e configuração. O fornecedor não é decidido pelo +Compras.
- **Estado atual:** `OpenAIProvider` permanece o adaptador implementado em Infrastructure; agentes e regras de negócio continuam dependentes apenas das abstrações.

## Resumo executivo

O BlueprintOS possui uma fundação backend validada para runtime de IA, agentes simples, conhecimento em Markdown, memória e estratégia de negociação em processo, workflow sequencial e publicação/documentação. O +COMPRAS possui CRUD de fornecedores e sincronização ERP operacional validados; ainda não há autenticação corporativa.

## Ciclo atual

- **Fase real atual:** Fundação arquitetural (Fase 0) concluída. O projeto foi oficialmente replanejado para o **MVP 1.0** segundo a estratégia Frontend First — ver `.ai/ROADMAP.md`. A arquitetura permanece a mesma; apenas o roadmap/backlog/organização de Work Orders mudaram. Com a consolidação final da estratégia (Gates de Onda, política de datas, definição oficial de "Pronto", artefatos `+Compras Funcional`/`+Compras UX`), o projeto entra oficialmente em **modo de execução**: mudanças estruturais futuras só ocorrem por necessidade real identificada durante a implementação.
- **Última sprint comprovadamente concluída:** **O1.10 — Conclusão do Vertical Slice (11/08/2026)**, formalmente concluída — migração estrutural pura das últimas pastas horizontais (`pages/Negociacoes`/`pages/Configuracoes` → `negotiations/`/`settings/`, pasta `pages/` removida); `core/AppShell.tsx` passa a filtrar todos os itens de Administração (Usuários, Filiais, Centros de Custo, Unidades de Alocação) pela permissão RBAC real correspondente, mesmo padrão já usado em Perfis desde a O1.5. Sem alteração de comportamento funcional. Ver `.ai/work-orders/completed/O1.10-ConclusaoVerticalSlice.md` e seção própria em `.ai/BACKLOG.md`.
- **Sprint funcional comprovadamente concluída anterior:** O1.9 — Centro de Custo × Unidade de Alocação, Vínculo Real (11/08/2026). Ver `.ai/work-orders/completed/O1.9-CentroDeCustoXUnidadeDeAlocacaoNN.md`.
- **Sprint funcional comprovadamente concluída anterior:** O1.11 — Fundação Multi-Unidade de Negócio e Configuração (11/08/2026), 7/7 entregáveis. Ver `.ai/work-orders/completed/O1.11-FundacaoMultiUnidadeDeNegocioEConfiguracao.md`.
- **Sprint anterior concluída:** O1.12 — Workflow, Alçadas, Aprovação e Controle Orçamentário (11/08/2026), 4/4 entregáveis. Ver `.ai/work-orders/completed/O1.12-WorkflowAprovacaoAlcadasOrcamento.md`.
- **Sprint atual:** **O1.13.5 — Fundação dos Agents Especialistas Linx, CONCLUÍDA (11/08/2026)** — base de conhecimento persistente e versionada (`LinxKnowledgeEntry`) com proveniência explícita (Descoberto/Inferido/Validado/Aprovado), recuperação por busca (MVP textual/estruturado), leitor read-only de descoberta de schema do `SOMA_DESENV` (`LinxSchemaDiscoveryReader`, comprovadamente incapaz de escrita por teste de reflexão sobre o contrato), RBAC dedicado (`ConhecimentoLinx.Gerenciar`/`ConhecimentoLinx.Aprovar` — a promoção final a "Aprovado" nunca é concedida pela mesma permissão de quem registra/valida), e os dois papéis de Agent (`LinxErpSpecialistAgent`/`LinxDatabaseSpecialistAgent`) como consumidores dessa base, com defesa testada contra prompt injection/knowledge poisoning (conteúdo recuperado é sempre dado citado no prompt, nunca instrução). Migration `AddLinxKnowledgeO1135` aplicada ao banco de desenvolvimento. Backend: 626 → 682 testes unitários (+56), 7 integração inalterado, 689/689 aprovados; build limpo. Revisão de segurança sem achados CRITICAL/HIGH. Nenhum dos 41 entregáveis oficiais da Onda 1 corresponde a esta fundação (decisão de escopo já registrada) — progresso técnico inalterado. Ver `.ai/work-orders/completed/O1.13.5-FundacaoAgentsEspecialistasLinx.md`.
- **Sprint anterior concluída:** **O1.13 — Administração Operacional e Monitoramento, CONCLUÍDA (11/08/2026)** — telas reais de Monitor de Integrações, Monitor de Filas/Reprocessamentos e Auditoria/histórico de sincronizações, cobrindo os entregáveis #29, #30, #31 e #32, consumindo exclusivamente dados reais de sincronização de fornecedores já persistidos por B2.1.3 (`SincronizacaoFornecedor`/`ErroSincronizacaoFornecedor`/`FornecedorSincronizacao`) — sem mock, sem novo motor de sincronização. Aproveitada a sprint para quitar a dívida técnica de RBAC ausente desde O1.5/B2.1.3 em `FornecedoresController`/`FornecedorSyncController` (enforcement real via `Fornecedor.Criar/Editar/Aprovar` e `Sistema.Gerenciar`, sem criar permissão nova). Nenhuma migration necessária (leitura pura). Revisão de segurança sem achados CRITICAL/HIGH/MEDIUM. Backend: 621 → 626 testes (+7 integração inalterado); frontend: 110 → 116 testes; builds limpos. Work Order movida para `completed/`. Nenhuma sprint funcional está ativa nesta sessão; abertura da O1.13.5 requer nova autorização explícita. Detalhe completo em `.ai/CURRENT_SPRINT.md`.
- **Sprint atual:** **O1.14 — Blueprint de Banco e Validação Funcional Final, CONCLUÍDA (11/08/2026)** — última sprint planejada da sequência atual da Onda 1. Consolidou (sem redigir do zero) o blueprint administrativo incremental de O1.5–O1.13.5 em `docs/database/Database.md` (fatias de Unidades de Alocação/CC×UA, Multi-BU, Workflow/Alçada/Orçamento, Fornecedores e Conhecimento Linx; catálogo RBAC completo com mapeamento de enforcement; fonte canônica ERP × +Compras por domínio; mapa de relacionamentos; matriz de rastreabilidade Domínio → Tabela → API → Frontend; estado de migrations sem drift). Produziu `docs/audits/O1.14-InventarioDividasEGaps.md` (18 dívidas técnicas + 4 GAPs consolidados, nenhuma quitada nesta sprint). Auditoria técnica direta identificou 1 achado MEDIUM novo (DEB-03, não bloqueante) e nenhum CRITICAL/HIGH; reconciliou explicitamente Agents Linx (O1.13.5) × AI Factory, confirmando que RAG/embeddings/orquestração multi-agente/observabilidade completa permanecem arquitetura-alvo sem implementação. Backend: `dotnet build` 0 erros/0 avisos, `dotnet test` 689/689 aprovados (sem regressão). Frontend não alterado. **A Onda 1 NÃO foi declarada concluída** — resta a etapa separada de auditoria exaustiva dos 41 entregáveis, implementação de GAPs, hardening e Gate Final. Ver `.ai/work-orders/completed/O1.14-BlueprintDeBancoEValidacaoFuncionalFinal.md`.
- **Etapa atual: Gate Final da Onda 1, executado em 12/08/2026 — GATE ABERTO (39/41, 95,12%).** Auditoria exaustiva dos 41 entregáveis com revalidação individual de evidência: 11 reclassificados para Concluído (#4, #5, #6, #7, #8, #11 — observações desatualizadas confirmadas tecnicamente satisfeitas; #36–#40 — blueprint por tela, matrizes tela×campo×entidade e +Compras×ERP, mapeamento de 92 endpoints, mapeamento de integrações, produzidos nesta etapa). Correção de segurança DEB-03 (isolamento cross-BU real em `SincronizacaoFornecedor`, com migration e 2 testes de regressão). Dívidas quitadas: DEB-15/M2 (transação atômica Usuário×Centro de Custo) e DEB-16 (propósitos de criptografia `DataProtection` separados por domínio); DEB-13 reclassificada SUPERADA (investigada, não reproduz). Demais 13 dívidas avaliadas e mantidas por decisão objetiva de custo/risco/escopo (ver `docs/audits/O1.14-InventarioDividasEGaps.md`). Testes finais: backend 693 unitários + 7 integração (700/700), frontend 116/116, builds limpos. **Restam 2 itens que impedem 41/41:** #9 (decisão indispensável do Product Owner — catálogo de Perfis de negócio, não corrigível tecnicamente) e #41 (validação funcional com os envolvidos — impedimento externo real: ambiente de execução sem VPN/acesso ao banco corporativo `MaisComprasConnection`). Achado registrado para decisão de produto (não bloqueante): 5 controllers administrativos recebem `unidadeNegocioId` do path (design documentado de administração corporativa cross-BU), mesma tensão arquitetural de `Sistema.Gerenciar` já levantada por DEB-03 — nenhuma exploração identificada. **Design Review consolidado e Onda 2 permanecem NÃO iniciados**, conforme escopo deste Gate.
- **Continuação do Gate Final da Onda 1 (12/08/2026) — hardening administrativo Produto × BU e catálogo de Perfis (entregável #9), GATE ABERTO (40/41, 97,56% exato, exibido 98%).** Formalizada, de forma central e reutilizável, a distinção entre RBAC (permissão — "o quê") e escopo administrativo (BU — "onde"), decisão do Product Owner: **Administrador Sênior = escopo `Produto`, cross-BU legítimo e explicitamente autorizado pelo backend; Administrador de BU (e demais Perfis de negócio) = escopo `Negocio`, restrito à própria Unidade de Negócio mesmo conhecendo o Id de outra.** Implementação: `EscopoAdministrativo` (enum de domínio) propagado por claim própria (`maiscompras_escopo_administrativo`, nunca confundida com `maiscompras_permissao`) desde `ObterIdentidadeAtualUseCase`/`SessionCookieAuthenticationHandler`; abstração central `EscopoAdministrativoUnidadeNegocio` (`BlueprintOS.Api.Authorization`) com `Autoriza(...)` e `TryResolverUnidadeNegocio(...)`, nunca uma comparação textual de `Perfil.Nome` espalhada por controllers. Os 5 controllers com `unidadeNegocioId` explícito no path (Alçadas, Regras de Workflow, Regras Orçamentárias, Identity Providers, Configuração de Notificações) passam a validar o alvo via `EscopoUnidadeNegocioPathFilter` no `MapGroup` — o achado anterior (cross-BU teoricamente possível para quem possuísse a permissão do recurso) está **resolvido**, não apenas "registrado para decisão futura". `PerfisController`/`UsuariosController` (2 dos 12 controllers "BU da sessão") passam a aceitar `unidadeNegocioId` opcional por query, honrado somente para escopo `Produto` — os demais 10 permanecem estritamente na BU da sessão por não haver necessidade funcional identificada nesta rodada (arquitetura já suporta a extensão, sem redesenho de UI). Entregável **#9 resolvido**: catálogo inicial de 5 Perfis de negócio (Administrador Sênior — já existente, ciclo de vida exclusivo do Bootstrap — mais **Administrador de BU, Comprador, Aprovador, Requisitante**, criados por `CatalogoInicialPerfisDeNegocioUseCase`, idempotente e multi-BU consciente), com matriz Perfil × Permissão derivada exclusivamente das 19 permissões reais do catálogo (nenhuma permissão de `Pedido.*` atribuída — sem enforcement/controller, GAP-01 inalterado; `Requisitante` nasce sem nenhuma permissão do catálogo atual, documentado como evolução futura). Seed: migration `AddCatalogoInicialPerfisDeNegocioO1Gate` (backfill idempotente via `NOT EXISTS`, mesmo padrão de `AddRbacPerfilPermissaoCatalogo`) para BUs existentes; `CatalogoInicialPerfisDeNegocioUseCase` em código para BUs futuras (chamado por `ConcluirBootstrapUseCase` e `CriarUnidadeNegocioUseCase`, mesma transação). Nenhuma permissão PRODUTO (`UnidadeNegocio.Gerenciar`, `ConfiguracaoErp.Gerenciar`, `Sistema.Gerenciar`) foi concedida a `Administrador de BU`. **Achado de segurança identificado e corrigido nesta própria rodada:** como `EscopoAdministrativo.Produto` era derivado somente do nome do Perfil, a invariante de não-escalonamento de PERMISSÃO (ADR-0020) não bastava — um Administrador de BU com `Perfil.Gerenciar` podia criar/renomear um Perfil para "Administrador Sênior" na própria BU e, ao se vincular a ele, ganhar escopo Produto sem nunca ter recebido essa concessão (escalonamento de **escopo**, caminho distinto do de permissão). Corrigido reservando o nome em `CriarPerfilUseCase`/`AtualizarPerfilUseCase` (`RbacFalha.NomeReservado`, 403) — só o Bootstrap cria esse Perfil real. Testes: 15 novos (6 cenários de escopo cross-BU em `EscopoAdministrativoUnidadeNegocioTests` + 6 do catálogo em `CatalogoInicialPerfisDeNegocioUseCaseTests` + 3 do nome reservado em `PerfilUseCasesTests`), suíte reexecutada por completo — backend 710 testes unitários + 7 integração (0 falhas), `dotnet ef migrations has-pending-model-changes` sem pendências; frontend 116/116, `tsc -b` e `npm run build` limpos (campo informativo `escopoAdministrativo` acrescentado a `GET /auth/me`/`UsuarioAutenticado`, sem alteração de UI/Design Review). **Restam 2 itens que impedem 41/41:** #41 (validação funcional com os envolvidos — impedimento externo real, inalterado) e a extensão opcional de override de BU aos 10 controllers "BU da sessão" restantes (arquitetura pronta, não implementada por falta de necessidade funcional nesta rodada — não é dívida bloqueante). Progresso técnico da Onda 1: 39/41 (95,1220%) → **40/41 (97,5610% exato, exibido 98%)**; Contribuição da Onda 1 ao MVP 19,0244 → **19,5122 pontos** (20% × 97,5610%); Percentual Global do MVP 1.0 46,02% → **46,5122% exato** (Foundation 20,0 + Onda 1 19,5122 + Onda 2 7,0), exibição convencional **47%**. **Por instrução expressa desta rodada, `.ai/dashboard/DASHBOARD_STATE.md` não foi editado** — mesmo precedente de O1.6/O1.7/O1.8: este recálculo vive em `PROJECT_STATE.md`/`CURRENT_SPRINT.md`/`BACKLOG.md`/`docs/audits/O1.14-InventarioDividasEGaps.md`; a atualização do Dashboard oficial permanece com o Product Owner. **Design Review e Onda 2 permanecem NÃO iniciados.**
- **Validação Funcional do entregável #41 (12/08/2026, mesma data) — GATE PERMANECE ABERTO EM 40/41 (97,56%, exibido 98%); validação TÉCNICA aprovada, aceite formal do Product Owner com os envolvidos ainda pendente.** VPN corporativa e `MaisComprasConnection`/`SOMA_DESENV` disponibilizadas pelo Product Owner nesta sessão — primeira conexão real de toda a sequência de Gates. Conectividade confirmada (TCP + login real em ambos os bancos); `dotnet ef database update` aplicado ao banco real (2 migrations pendentes da rodada anterior + 2 migrations de correção desta rodada). Backend e frontend reais executados (`dotnet run` + `vite`); jornadas funcionais validadas via Chrome DevTools MCP contra a aplicação real: autenticação OTP real (fluxo de produção, não bypass), sessão/`/auth/me`/escopo administrativo, Administrador Sênior (Perfis, Usuários, Filiais, Centros de Custo — dados reais do ERP, Unidades de Alocação, as 6 configurações Multi-BU, Workflow/Alçadas/Orçamento, Fornecedores, Monitoramento, Agentes IA), e um Administrador de BU real recém-criado (isolamento negativo cross-BU). **4 bugs reais encontrados e corrigidos nesta sessão, todos invisíveis sem uma conexão real** (banco, ERP ou navegador — nenhum detectável por leitura de código, testes com mocks, ou os Gates anteriores sem VPN): (1) Administrador Sênior real com apenas 14/19 permissões do catálogo (migrations de permissão posteriores a O1.5 nunca reexecutaram o backfill) — `FixAdministradorSeniorPermissoesCompletasO1Gate`; (2) `/me` nunca esteve no proxy de desenvolvimento (`vite.config.ts`) — Seleção de Unidade de Negócio (#3) sempre quebrada em DEV; (3) **DEB-13 reaberta** — a conclusão anterior "SUPERADA/não reproduz" (Gate Final original, sem VPN) estava errada: `FornecedorConfiguration.cs` mapeava `RazaoSocial`/`Cnpj_Cpf` para colunas físicas `Nome`/`Cnpj` que a migration `B212FornecedorLinxCanonicalModel` já havia renomeado — `GET /fornecedores` sempre retornava `500`; corrigida removendo os `HasColumnName` incorretos, com teste de regressão de metadados EF (`FornecedorColumnMappingTests`); (4) **DEB-19 (nova)** — `SomaCentroCustoReader` configurado para a tabela `CENTRO_CUSTO`, inexistente no SOMA_DESENV real (a real é `CENTROS_CUSTO`) — `GET /api/administracao/centros-custo` sempre retornava `500`; corrigido no `appsettings.json` e nos aliases de coluna do reader, confirmado end-to-end com dados reais do ERP (lojas AZZAS). Revalidada em produção real, com dois atores reais e duas Unidades de Negócio reais (incluindo uma nova BU criada durante a sessão, com catálogo de Perfis auto-seedado confirmado no banco): a proteção de nome reservado (ADR-0022) e os 6 cenários de escopo cross-BU — todos com o resultado esperado (`403 nome_reservado`, `403 escopo_administrativo_negado`, `200` para acesso legítimo). Testes reexecutados por completo com as conexões reais habilitadas: backend 713 unitários + 7 integração (0 falhas, todas as 7 exercitando conexão real pela primeira vez em vez de early-return silencioso), `dotnet ef migrations has-pending-model-changes` sem pendências; frontend 116/116, `tsc -b` e `npm run build` limpos. **Decisão sobre #41:** a definição formal do entregável (`docs/audits/Onda1-Reconciliacao-e-Plano-Execucao.md`; `DASHBOARD_STATE.md`: "sessão de validação **com os envolvidos**"; "Aprovação formal da Onda 1 pelo Product Owner, após resolução de #9 e #41") exige uma sessão com stakeholders humanos, não apenas execução técnica pelo agente — por isso **#41 permanece Planejado/pendente**, não reclassificado para Concluído nesta rodada, apesar de toda a validação técnica ter sido aprovada e todos os bugs encontrados corrigidos e retestados. Nenhum aceite foi autoassinado em nome do Product Owner. **Onda 1 permanece em 40/41 (97,5610% exato, exibido 98%); MVP global inalterado em 46,5122% (exibição 47%)** — nenhum destes 4 bugs alterava a matriz de entregáveis Concluído/Em desenvolvimento/Planejado (eram regressões de qualidade dentro de entregáveis já classificados Concluído, não GAPs de escopo). **`.ai/dashboard/DASHBOARD_STATE.md` não foi editado.** **Design Review e Onda 2 permanecem NÃO iniciados.**
- **Fechamento Formal da Onda 1 (12/08/2026, mesma data) — ACEITE DO PRODUCT OWNER RECEBIDO. GATE FINAL CONCLUÍDO. ONDA 1 — 41/41 (100%).** O Product Owner registrou aceite formal explícito sobre o Relatório Final da Validação Funcional do Entregável #41 (sessão imediatamente anterior desta mesma data), nestes termos: aprova a validação funcional da Onda 1; autoriza marcar #41 como Concluído; autoriza reconciliar a matriz para 41/41; autoriza registrar o progresso técnico como 100%; autoriza encerrar o Gate Final; autoriza declarar a Onda 1 formalmente Concluída. O aceite considera como evidência válida a validação real já produzida (ambiente DEV real, `MaisComprasConnection`/`SOMA_DESENV` reais, login OTP real, jornadas funcionais via Chrome DevTools MCP) e confirma que os 4 bugs encontrados (Administrador Sênior com permissões incompletas; proxy Vite sem `/me`; DEB-13 reaberta — mapeamento de coluna do Fornecedor; DEB-19 — tabela de Centro de Custo mal configurada) foram corrigidos e retestados antes deste aceite. Não foi exigido ambiente de Homologação para este fechamento.
  - **Entregável #41 — Validação funcional com os envolvidos: CONCLUÍDO.** Evidência: conectividade real com `MaisComprasConnection` e `SOMA_DESENV`; backend/frontend reais; login OTP real; sessão e `/auth/me`; Administrador Sênior cross-BU (leitura e escrita, incluindo criação de nova Unidade de Negócio com catálogo de Perfis auto-seedado); Administrador de BU real com isolamento cross-BU negativo confirmado; Perfis/RBAC (incluindo proteção de nome reservado, ADR-0022); Usuários; Filiais; Centros de Custo (dados reais do ERP); Unidades de Alocação; Centro de Custo × Unidade de Alocação; as 6 configurações Multi-BU; Workflow; Alçadas; Orçamento; Fornecedores; Monitoramento; fundação dos Agents Linx (O1.13.5, escopo já entregue); Chrome DevTools MCP; correção e reteste dos 4 bugs encontrados; 713 testes unitários backend + 7 de integração (0 falhas); 116 testes frontend; builds limpos em ambos; migrations sincronizadas (`dotnet ef migrations has-pending-model-changes` sem pendências). Esta evidência não foi reproduzida nesta rodada — é a mesma já produzida na sessão de Validação Funcional imediatamente anterior; nenhuma alteração de código foi feita nesta rodada de fechamento, portanto os testes não foram reexecutados.
  - **Matriz final da Onda 1: 41 entregáveis / 41 Concluídos / 0 Em desenvolvimento / 0 Planejado.**
  - **Progresso Técnico da Onda 1: 41 ÷ 41 = 100% exato.** Contribuição da Onda 1 ao MVP: Peso Gerencial (20%) × Progresso Técnico (100%) = **20,0 pontos** (antes 19,5122). **Percentual Global do MVP 1.0: 46,5122% → 47,0% exato** (Foundation 20,0 + Onda 1 20,0 + Onda 2 7,0), exibição **47%** — mesma fórmula oficial já usada em todos os recálculos anteriores deste documento, sem fórmula nova.
  - **Gate Final da Onda 1: ABERTO → CONCLUÍDO.** Resumo do que o Gate cobriu, do início ao fechamento: auditoria exaustiva dos 41 entregáveis com revalidação individual de evidência; dívidas técnicas revisadas e classificadas (`docs/audits/O1.14-InventarioDividasEGaps.md`); hardening administrativo Multi-BU (ADR-0022 — escopo administrativo Produto × BU, formalizado e testado); catálogo inicial de Perfis de negócio concluído (#9); validação funcional real realizada (#41); aceite formal do Product Owner recebido. **41/41 entregáveis Concluídos.**
  - **ONDA 1 — FORMALMENTE CONCLUÍDA.**
  - **Dívidas remanescentes preservadas, não quitadas por este fechamento** (nenhuma dívida técnica legítima foi apagada ou reclassificada sem correção real — ver `docs/audits/O1.14-InventarioDividasEGaps.md` para o detalhamento completo de cada uma): DEB-02 (Fornecedor.BusinessUnit sem FK formal Multi-BU, não bloqueante em mono-BU real), DEB-04 (FK física ausente nas tabelas Multi-BU + granularidade de `Sistema.Gerenciar`), DEB-05/DEB-06/DEB-08/DEB-09/DEB-10/DEB-11/DEB-12/DEB-14/DEB-17 (mantidas por decisão objetiva de custo/risco/escopo já registrada, ressalvas de segurança já aceitas formalmente nas sprints de origem), GAP-02/GAP-03 (especificação de UX/produto completa por tela — escopo do Design Review consolidado pós-Onda 1, ainda NÃO iniciado), GAP-01/GAP-04 (módulo de Pedido e motor de aprovação operacional — Onda 3/4, fora do escopo da Onda 1), DEB-18 (evolução RAG/embeddings dos Agents Linx — arquitetura-alvo declarada, não dívida). Os 2 gates de Homologação (Cache-Control `no-store` em `/bootstrap/*`; validação de entropia do Bootstrap Secret) continuam bloqueando explicitamente apenas a promoção para Homologação, não a conclusão da Onda 1.
  - **Bugs da validação funcional preservados no histórico do Gate** (não removidos): os 4 bugs reais encontrados na sessão de Validação Funcional do #41 (Administrador Sênior com permissões incompletas; proxy Vite sem `/me`; DEB-13 reaberta; DEB-19 nova) permanecem registrados em `docs/audits/O1.14-InventarioDividasEGaps.md`, seção "Achados da Validação Funcional #41", como evidência da efetividade do Gate — todos corrigidos e retestados antes deste fechamento.
  - **Handoff para o Design Review Consolidado Pós-Onda 1 (documentado, NÃO iniciado nesta rodada):** próxima etapa decidida pelo Product Owner — (1) auditoria visual do aplicativo SOMA real via Chrome, após login manual do Product Owner (sessão futura; Claude não deve capturar/exibir/persistir token, cookie ou credencial — apenas usar a sessão já autenticada para leitura visual); comparar padrão GDT/AZZAS documentado × padrões reais do SOMA × implementação atual do +Compras; produzir Padrão Observado → Estado Atual → GAP → Recomendação; (2) revisão de arquitetura de informação e navegação: sidebar principal organizada por contexto, submenus expansíveis, sidebar recolhível, ícones sóbrios/monocromáticos, destaque de cor principalmente para estado ativo; (3) configurações acessadas por engrenagem, respeitando `EscopoAdministrativo` (Administrador Sênior → Produto + BU; Administrador de BU → Negócio da própria BU); (4) header com componente único de identidade do usuário (avatar/iniciais, nome, e-mail, Perfil/Perfis, BU atual, Escopo Administrativo, troca de BU quando autorizada, Sair); (5) manter navegação principal lateral (não migrar tudo para menu superior); (6) separar operação de Compras da Administração; (7) revisar agrupamentos conceituais (Compras, Cadastros, Organização, Acessos, Monitoramento, Configurações); (8) preservar Design System AZZAS/SOMA. Nenhuma dessas mudanças foi implementada nesta rodada — apenas documentada como próximo passo.
  - **Confirmações explícitas de fechamento:** auditoria visual do SOMA **NÃO** iniciada; Design Review **NÃO** iniciado; Onda 2 **NÃO** iniciada; `[atualizar dashboard]` **NÃO** executado (`.ai/dashboard/DASHBOARD_STATE.md` permanece D-1, por decisão operacional do Product Owner); workflow n8n **NÃO** tocado.
- **Progresso real:** documentação/publicação, capacidades internas de IA e um fluxo consultivo de negociação por API estão implementados; os demais fluxos de produto +COMPRAS e os requisitos de operação corporativa permanecem pendentes.

## Capacidades implementadas

| Área | Evidência no código | Estado |
|---|---|---|
| AI Runtime | `IAIRuntime`, `OpenAIProvider` e contratos de chat | Implementado |
| Agents | `IAgent`, `BaseAgent`, `EchoAgent`, `KnowledgeAgent`, `AgentFactory` | Implementado, básico |
| Knowledge | `MarkdownKnowledgeProvider` e `KnowledgeService` | Implementado, baseado em Markdown |
| Negociação | `NegotiationMemory`, regras e `NegotiationStrategy` | Implementado, em memória |
| API de negociação | `POST /api/v1/negociacoes/recomendacoes` via `NegotiationRecommendationUseCase` | Implementado, consultivo e sem estado |
| Fornecedores | `Fornecedor`, EF Core/SQL Server sobre `MaisComprasConnection`, migration e `POST/GET/PUT/DELETE /fornecedores` | Implementado |
| Sincronização de fornecedores | Contrato canônico, adaptadores por BU, importação/exportação/inativação, `LX_SEQUENCIAL`, timestamp Linx, concorrência, idempotência, auditoria append-only, modelo Linx alinhado, fluxo operacional ERP SOMA → +Compras, lotes paginados com teto operacional real, histórico de execução, erros parciais e métricas | B2.1.3 concluída e validada em 02/08/2026 contra Docker/VPN/`MaisCompras` reais |
| Enriquecimento de fornecedores por CNPJ | `ICnpjConsultaProvider`, `ConsultaCnpjResultado`, `BrasilApiCnpjProvider`, análise de divergências, aprovação/rejeição, atualização seletiva, `FornecedorEnriquecimentoAnalise`, endpoint `POST /fornecedores/consulta-cnpj` e tela React `CadastroFornecedor` | B2.2 concluída |
| Portal +Compras (frontend) | AppShell, rotas React Router, `ApprovalPanel.tsx`, `SupplierComparison.tsx`, módulo Fornecedores conectado à API real; demais módulos demonstrativos | Concluído no frontend (commit `8ee8f4e`); backend não revalidado neste ciclo (sem SDK .NET no ambiente) |
| Descoberta de fornecedores | `FornecedorDescoberto`, score centralizado, leitura `SOMA_DESENV`, persistência +Compras e `/api/fornecedores/descobertas` | Implementado; validação SQL ERP pendente de ambiente com acesso |
| Workflow | `Workflow` e `WorkflowRunner` sequenciais | Implementado, básico |
| Documentation | contratos, geradores, publicação Markdown, Git reader e health report | Implementado |
| Knowledge Linx (Agents Especialistas) | `LinxKnowledgeEntry` (EF Core/SQL Server, proveniência e versionamento), `LinxErpSpecialistAgent`/`LinxDatabaseSpecialistAgent`, `LinxSchemaDiscoveryReader` (read-only `SOMA_DESENV`), RBAC dedicado (`ConhecimentoLinx.Gerenciar`/`ConhecimentoLinx.Aprovar`) | Fundação implementada (O1.13.5) — sem ingestão em massa de documentação/OBJ, sem RAG/embeddings (evolução futura) |
| Publication | renderização Markdown/HTML/PDF, QR Code e publicadores por público | Implementado |

## Capacidades parciais

- **Memória:** existe apenas a memória de negociação em processo; não há memória corporativa genérica, persistência nem recuperação de longo prazo.
- **API:** host ASP.NET Core, OpenAPI em desenvolvimento, `GET /health` e o endpoint consultivo de negociação existem; não há autenticação corporativa, autorização ou contratos para os demais domínios de Procurement.
- **Infraestrutura:** EF Core/SQL Server possui `BlueprintOSDbContext`, migration inicial, conexões segregadas de +Compras/ERP e validador somente leitura; não há CI/CD, GCP, Kubernetes, Terraform, Nginx ou observabilidade implementados.
- **Arquitetura:** o estilo alvo é Modular Monolith com módulos por camada, mas o código real permanece em projetos transversais `Core`/`Infrastructure`.

## Capacidades não iniciadas

- Identity, autorização, multi-tenant e Microsoft Entra ID.
- Planner, Procurement, Notifications, Dashboard e Analytics.
- Portal +COMPRAS completo; somente fornecedor está funcional e conectado ao backend em `frontend/web`.
- n8n e APIs corporativas; integração ERP de fornecedores B2.1 está implementada.

## Agentes e integrações concretos

- **Agentes:** `EchoAgent`, `KnowledgeAgent`, e (O1.13.5) `LinxErpSpecialistAgent`/`LinxDatabaseSpecialistAgent` — especialistas de conhecimento ERP/Banco Linx, consumidores da base `LinxKnowledgeEntry`, nunca executores de SQL de escrita. Não existe classe concreta `SeniorBuyerAgent`, `NegotiationAgent`, `ComplianceAgent` ou `RiskAgent`.
- **Integrações:** OpenAI Chat Completions via `OpenAIProvider`; descoberta e sincronização de fornecedores via adaptadores em `SOMA_DESENV`; CLI Git somente para leitura de histórico de documentação.
- **Identidade temporária:** `DevelopmentRequestIdentity` atende somente Development e alimenta `ICurrentIdentity`; fornecedores persistem esse vínculo sem dependência da implementação concreta.

## Qualidade

| Suíte | Executados | Aprovados | Ignorados | Falhos |
|---|---:|---:|---:|---:|
| Unitários | 693 | 693 | 0 | 0 |
| Integração | 7 | 7 | 0 | 0 |
| Total | 700 | 700 | 0 | 0 |

> Tabela atualizada em 12/08/2026 (Gate Final da Onda 1) por execução real de `dotnet build`/`dotnet test backend/BlueprintOS.sln`: build aprovado com 0 erros e 0 avisos; testes aprovados, 0 falhas (682 → 693 testes unitários, +11 dos testes de regressão de DEB-03, DEB-15/M2 e DEB-16). Frontend (`frontend/web`) reexecutado nesta sessão: `npm run build` (`tsc -b && vite build`) sem erros, `npm test` (Vitest) 116/116 aprovados, sem regressão.

Build da solution: `dotnet build backend/BlueprintOS.sln` executado em 03/08/2026, sucesso, 0 erros e 0 avisos. Build do frontend: `npm run build` (`tsc -b && vite build`) executado em 03/08/2026, sucesso.

## Riscos e pendências

- Apenas a recomendação de negociação está exposta por API; os demais domínios de negócio ainda não possuem API ou interface utilizável.
- A atualização do nome no ERP permanece limitada pela FK `FORNECEDORES.FORNECEDOR → CADASTRO_CLI_FOR.NOME_CLIFOR`; a B2.1 validou atualização de CNPJ sem operação destrutiva.
- Dados de negociação e documentação ainda não são duráveis.
- A configuração da chave OpenAI depende de ambiente e não há tratamento operacional completo para credenciais, rate limits ou telemetria.
- A arquitetura física diverge do layout alvo; uma migração deve ser planejada somente quando trouxer benefício concreto.
- Métricas e estado de documentação exigem atualização a cada sprint até existir automação de CI.
- Portal +Compras Frontend (commit `8ee8f4e`) teve apenas o frontend validado (`tsc`/`vite build`, 4/4 testes) neste ciclo; `dotnet build`/`dotnet test` do backend não foram executados por falta de SDK .NET no ambiente de revisão e seguem pendentes de execução local antes de encerrar a frente.
- B2.1.3: concluída e validada em 02/08/2026 (build, testes e execução real contra VPN/`MaisCompras`, à época via Docker). Ver `.ai/memory/completed_sprints.md` para o detalhamento dos três problemas encontrados e corrigidos nessa validação, e `docs/audits/B-Series-Reconciliation.md` para o levantamento anterior (histórico, pré-validação real).
- Remoção do Docker (03/08/2026): concluída e auditada. Docker deixou de ser parte do fluxo de desenvolvimento; o ambiente oficial é local, sem containers (ADR-0018 atualizada). Nenhum resíduo funcional foi encontrado na auditoria final.

### Riscos encerrados (mitigados)

- ~~**Arquitetura Frontend indefinida**~~ — encerrado pela Revisão Arquitetural R2 (06/08/2026): Vertical Slice aprovada e adotada como padrão obrigatório (ADR-0020), com fundação física implementada (O1.2.1) e todos os módulos administrativos da Onda 1 já nascendo nesse padrão.
- ~~**Modelo Administrativo (corte Administração/Administração do Sistema/Configurações, RBAC, cadastros integrados) indefinido**~~ — encerrado pela Revisão Arquitetural R1 (R1.1+R1.2, 06/08/2026) e pela conclusão/aprovação do Gate Administrativo (06/08/2026): ADR-0020 consolidou o modelo; os cinco módulos administrativos da Onda 1 (Perfis, Usuários, Filiais, Centros de Custo, Unidades de Alocação) estão implementados como fundação visual mockada.
- Os riscos residuais de detalhamento já registrados na ADR-0020 (relação N:N Centro de Custo × Unidade de Alocação a modelar antes da Onda 5; nomenclatura de tela incorreta em materiais informais) permanecem válidos como registro histórico da decisão — não são reabertos por este encerramento, e não são alterados aqui por pertencerem ao texto da ADR.

### Risco ativo — Dependência da Infra (provedor de e-mail para OTP)

- **Descrição:** definição/contratação do provedor corporativo de e-mail transacional para o envio de código OTP no Login Passwordless da Onda 1 (O1.4) ainda não foi feita pela Infra.
- **Impacto:** alto para o cronograma de O1.4.2 — sem provedor definido, a implementação de Login/OTP não pode ser codificada nem testada; bloqueia a conclusão da frente de Autenticação e, por consequência, o Gate final da Onda 1.
- **Probabilidade:** média — depende exclusivamente de uma decisão/contratação externa ao escopo do +Compras, sem estimativa de prazo registrada nesta atualização.
- **Plano de mitigação:** escalar a decisão à Infra como bloqueador explícito **de Homologação**, não mais de início de O1.4.2 (Development Auth Strategy, O1.4.1.1, `docs/architecture/security-design-auth-o1.4.md`, seção 17) — a implementação de O1.4.2 usa um `DevelopmentOtpEmailSender` exclusivo de Development, com fail-closed fora dele, permitindo que o restante da especificação técnica (modelo de sessão, RBAC, Bootstrap, fluxo OTP completo) avance sem esperar o provedor corporativo. A dependência da Infra é reclassificada como item do Authentication Infra Readiness Gate (seção 17.7), obrigatório antes de Homologação, não antes de O1.4.2.

## Ambiente de execução

- **Decisão vigente (ADR-0018):** o Portal +Compras é desenvolvido e validado em ambiente de **Desenvolvimento Local**, no Mac do desenvolvedor — frontend React via Vite (`http://localhost:5173`) e API .NET via `dotnet run` (`http://localhost:5262`), sem Docker.
- Persistência aponta para o SQL Server corporativo (`SOMA_DESENV`), acessado via VPN; não se usa SQL Server em container para o fluxo principal. Connection strings seguem configuráveis via user-secrets/variáveis de ambiente, sem valores fixos no repositório.
- CORS do backend liberado apenas para as origens locais `http://localhost:5173` e `http://127.0.0.1:5173`.
- Uma tentativa anterior de publicar o frontend via n8n (com backend exposto por túnel ngrok) foi revertida: o n8n só serve HTML como string única e não havia ambiente de backend publicado além de localhost. Publicação via n8n/GCP passa a ser tratada como opção futura de homologação, não como ambiente corrente.

## Revisão Arquitetural R1 (R1.1 + R1.2) — Modelo Administrativo, Cadastros Integrados, Segurança RBAC e Autenticação — concluída

- **Status:** a **Revisão Arquitetural R1 está concluída** — suas duas etapas, **R1.1 concluída** e **R1.2 concluída**, com todas as decisões consolidadas em uma única ADR. **Arquitetura aprovada** para o escopo revisado. A **O1.2 está liberada para início**, sujeita às pendências registradas abaixo e à aprovação do Product Owner para abertura formal da Work Order.
- **Decisão aceita:** [ADR-0020](./DECISIONS.md) — criada pela R1.1 e atualizada pela R1.2 — registra oficialmente: (1) corte definitivo entre `Administração`, `Administração do Sistema` e `Configurações`, substituindo a distribuição provisória da Work Order O1.1; (2) regra de cadastros integrados do ERP (Filiais, Centros de Custo) como imutáveis no +Compras, com metadados locais opcionais; (3) `Gestão de Filiais` e `Gestão de Centros de Custo` como nomes funcionais oficiais (nunca "Cadastro de..."); (4) Unidades de Alocação como substituto formal do conceito informal "Gestão de Empresas"; (5) relação N:N entre Centro de Custo e Unidade de Alocação, com Unidade de Alocação padrão; (6) separação entre cadastro mestre de Centro de Custo e autorização de acesso do usuário; (7) modelo de segurança RBAC exclusivo por perfil, sem permissão individual ou exceção direta ao usuário; **(R1.2)** (8) consolidação dos dois vínculos de acesso do usuário (Perfis e Centros de Custo, independentes entre si); (9) reafirmação sem ambiguidade do RBAC exclusivo por perfil; (10) Login Passwordless via OTP por e-mail como mecanismo oficial da Onda 1, com Microsoft Entra ID coexistindo no futuro; (11) Bootstrap Mode para inicialização do sistema sem Administrador Sênior cadastrado, encerrado permanentemente após uso; (12) exigência obrigatória de revisão do Agente Engenheiro de Segurança Sênior — antes e depois da implementação — para toda funcionalidade de autenticação. A R1.2 também corrigiu nomenclatura: `Notificações de negócio` → `Notificações`; `Monitor de filas` → `Monitor` e `Filas` (sub-telas distintas); `Saúde do sistema` → `Saúde`.
- **Reconciliação documental (encerramento da R1):** confirmada a integridade da ADR-0020 (nenhuma divergência encontrada entre a ADR e as decisões aprovadas); criado o documento permanente [Domain Principles](../docs/architecture/domain-principles.md), consolidando os princípios de domínio já aprovados (plataforma, dados mestres, administração, cadastros integrados, segurança), sem duplicar conteúdo de ADR; revisão cruzada entre ADR-0020, Domain Principles, `ENGINEERING_BLUEPRINT.md`, `ComprasFuncional.md`, `ComprasUX.md` e `ComprasDataModel.md` sem contradições ou nomenclatura divergente encontradas.
- **Natureza das revisões:** exclusivamente documentais — nenhum código, migration, frontend ou backend foi alterado em nenhuma delas. Documentos atualizados: `docs/product/ComprasFuncional.md`, `docs/product/ComprasUX.md`, `docs/product/ComprasDataModel.md`, `.ai/ENGINEERING_BLUEPRINT.md`, `.ai/DECISIONS.md` (ADR-0020), `docs/architecture/domain-principles.md` (novo), `docs/README.md` (índice).
- **Pendências para a O1.2:** conteúdo do catálogo de Perfis e Permissões (incluindo o perfil "Administrador Sênior" exigido pelo Bootstrap); provedor de e-mail transacional para o OTP; processo de acionamento do Agente Engenheiro de Segurança Sênior; especificação funcional detalhada das sub-telas novas ainda sem UX/dados completos (`Notificações`, `Integrações`, `Monitor`, `Filas`, `Reprocessamentos`, `Auditoria`, `Logs`, `Saúde`, `Conta`, `Preferências`, `Tema`, `Idioma`) — ver "Dúvidas de produto" em `ComprasFuncional.md`.
- **Dashboard:** estas revisões não executam o comando `[atualizar dashboard]`; `DASHBOARD_STATE.md` não foi alterado. A execução futura do comando é necessária para refletir a ADR-0020 (R1.1 e R1.2) e o Domain Principles no Dashboard Oficial.

## Sprint O1.3.3 — Fundação funcional do módulo Gestão de Filiais (06/08/2026) — ✅ concluída

- **Status:** ✅ concluída e encerrada oficialmente, com aprovação explícita do Product Owner. Módulo `administration/branches` disponível, aplicando a regra de cadastro integrado do ERP (ADR-0020, item 3): Filial é dado mestre do ERP, nunca criada ou alterada no +Compras — apenas ativada/inativada localmente, com Descrição +Compras opcional.
- **Arquitetura preservada:** implementação seguiu integralmente o padrão Vertical Slice (ADR-0020), no mesmo formato físico já usado por `administration/profiles`/`administration/users`.
- **Work Order:** `.ai/work-orders/completed/O1.3.3-GestaoDeFiliais.md`.
- **Próxima sprint:** O1.3.4 — Gestão de Centros de Custo, concluída.

## Sprint O1.3.4 — Fundação funcional do módulo Gestão de Centros de Custo (06/08/2026) — ✅ concluída

- **Status:** ✅ concluída e encerrada, com base em evidência técnica (código presente no working tree; `npm run build` e `npm run test` aprovados nesta atualização). Módulo `administration/cost-centers` disponível, aplicando a mesma regra de cadastro integrado do ERP (ADR-0020, item 3) já usada em `administration/branches`: Centro de Custo é dado mestre do ERP, nunca criado, editado ou excluído no +Compras — apenas ativado/inativado localmente, com Descrição +Compras opcional. O relacionamento com Unidade de Alocação (ADR-0020, item 5) é exibido apenas visualmente, com dados mockados — módulo `allocation-units` ainda não implementado.
- **Arquitetura preservada:** implementação seguiu integralmente o padrão Vertical Slice (ADR-0020), no mesmo formato físico já usado por `administration/profiles`/`administration/users`/`administration/branches`.
- **Validações:** `npm run build` (`tsc -b && vite build`) aprovado; `npm run test` (Vitest) aprovado, 25/25 testes (17 pré-existentes inalterados + 8 novos de `CentrosCustoPage`). Smoke test de navegação em browser real não executado neste ciclo (ambiente não-interativo, sem Chrome disponível); verificação estática confirma rota `/administracao/centros-custo` em `AppRoutes.tsx` e item de menu em `AppShell.tsx`.
- **Work Order:** `.ai/work-orders/completed/O1.3.4-GestaoDeCentrosDeCusto.md`.
- **Próxima sprint:** O1.3.5 — Unidades de Alocação (`allocation-units`), em andamento. Ver seção própria em `.ai/CURRENT_SPRINT.md`.

## Sprint O1.3.5 — Fundação funcional do módulo Gestão de Unidades de Alocação (06/08/2026) — ✅ concluída

- **Status:** ✅ concluída e encerrada formalmente, com aprovação explícita do Product Owner (registrada em 06/08/2026, revisão consolidada do Gate Administrativo). Módulo `administration/allocation-units` implementado como fundação visual mockada, aplicando a ADR-0020 (item 4): Unidade de Alocação pertence exclusivamente ao +Compras e nunca é integrada do ERP — ao contrário de Filial e Centro de Custo, existe cadastro completo (criação, edição, visualização, ativação/inativação, sem exclusão física).
- **Arquitetura preservada:** implementação seguiu integralmente o padrão Vertical Slice (ADR-0020), no mesmo formato físico já usado por `administration/profiles`/`administration/users`/`administration/branches`/`administration/cost-centers`.
- **Validações:** `npx tsc -b` (0 erros); `npm run build` (`tsc -b && vite build`) aprovado; `npm run test` (Vitest) aprovado, 31/31 testes (25 pré-existentes inalterados + 6 novos de `UnidadesAlocacaoPage`); smoke test real em navegador headless (Playwright) confirmou navegação, ausência de erro de import e funcionamento de listagem/criação/visualização/edição/ativação-inativação — único erro de console observado foi o HTTP 500 pré-existente do proxy Vite para a API na rota Dashboard (backend não estava em execução), já documentado desde a O1.2.1. Todas as evidências acima são as previamente registradas nesta sprint; não foram executadas novas rodadas de build/teste/smoke no encerramento formal.
- **Pendências remanescentes (não bloqueantes para o encerramento):** (1) relacionamento N:N entre Centro de Custo e Unidade de Alocação (ADR-0020, item 5) ainda não implementado de fato — cada lado permanece com dados mockados independentes, conforme escopo original desta sprint, permanece aberta; (2) ~~Work Order ainda não movida fisicamente para `.ai/work-orders/completed/`~~ — **regularizada em 06/08/2026** (Housekeeping Administrativo): arquivo movido para `.ai/work-orders/completed/O1.3.5-GestaoDeUnidadesDeAlocacao.md`; (3) ~~módulo `administration/users` possui `deleteUsuario` (exclusão física)~~ — **regularizada em 06/08/2026** (Housekeeping Administrativo): substituído por fluxo de ativação/inativação, consistente com os demais módulos administrativos.
- **Commit/push:** não realizado. Esta atualização documental encerra o registro do estado da sprint apenas nos arquivos de governança locais; nenhuma alteração foi commitada ou enviada ao repositório remoto.
- **Work Order:** movida para `.ai/work-orders/completed/O1.3.5-GestaoDeUnidadesDeAlocacao.md` em 06/08/2026 (Housekeeping Administrativo).

## Gate Administrativo — APROVADO / Frente O1.4 (Autenticação e Segurança) liberada (06/08/2026)

- **Gate Administrativo: APROVADO.** Housekeeping Administrativo concluído (ver `.ai/CURRENT_SPRINT.md`); fundação administrativa da Onda 1 (Perfis, Usuários, Filiais, Centros de Custo, Unidades de Alocação) completa em nível de fundação visual mockada, sem pendência de governança bloqueante remanescente. Nenhuma decisão de roadmap alterada por este registro.
- **O1.4.1 — Security Design Review: ✅ concluída (06/08/2026).** Revisão arquitetural de segurança e threat modeling exigida por ADR-0020 (item 13) antes de qualquer implementação de autenticação, sem nenhum código/endpoint/migration/OTP/Bootstrap implementado. Documento: [security-design-auth-o1.4.md](../docs/architecture/security-design-auth-o1.4.md).
- **Security Design Gate: APROVADO COM PENDÊNCIAS.** Implementação (O1.4.2) bloqueada até o Product Owner decidir: catálogo de Perfis/Permissões (incl. "Administrador Sênior"), provedor transacional de e-mail para OTP, e escopo de `Perfil`/`Permissao` (global vs. por Unidade de Negócio) — ver seção 12 do documento.
- **Próxima frente candidata:** O1.4.2 (Work Order técnica de implementação), condicionada aos bloqueadores acima.
- **O1.4.2.2 — Hardening do DevelopmentHeaderAuthenticationHandler: implementação técnica concluída (07/08/2026).** Correção pontual do Achado E (ALTO) encontrado pela segunda Security Validation independente: o handler passou a exigir `RemoteIpAddress` estritamente loopback, além da checagem de ambiente já existente — mesma defesa já usada em `/dev/otp`, agora aplicada consistentemente. Suíte de backend: 294→306 testes, todos aprovados; frontend inalterado. **A O1.4.2 permanece não concluída** — aguarda uma última Security Validation independente. Detalhe: `.ai/CURRENT_SPRINT.md` e `docs/architecture/security-design-auth-o1.4.md` §19.
- **O1.4.2.1 — Security Hardening da Autenticação OTP: implementação técnica concluída (07/08/2026).** Resposta aos 4 achados ALTO (rate limit por e-mail, atomicidade de OTP via RowVersion+índice único, secure-by-default via `AuthorizationOptions.FallbackPolicy`, hardening de `/dev/otp`) e ao achado MÉDIO/N (fail-closed comprovado por host real) da Security Validation da O1.4.2. Suíte de backend: 275→294 testes, todos aprovados; frontend inalterado, 38/38. **A O1.4.2 permanece não concluída** — aguarda nova Security Validation independente antes de Bootstrap/Administrador Sênior. Detalhe: `.ai/CURRENT_SPRINT.md` e `docs/architecture/security-design-auth-o1.4.md` §18.
- **O1.4.2 — Login Passwordless OTP e Sessão Segura: implementação técnica concluída (07/08/2026), pendente de Security Validation e aprovação do Product Owner (ADR-0020, item 13).** Backend (`Domain/Identity`, `Application/Identity`, `Infrastructure/Identity`, `Api/Auth`) e frontend (`auth/`) implementados; `dotnet build`/`dotnet test` (275/275) e `tsc`/`vite build`/`vitest` (38/38) aprovados; smoke test real em Chromium confirmou guarda de rota, headers de segurança, CSRF, rate limiting e ausência de dados sensíveis em storage. Fluxo de sucesso ponta a ponta (OTP real → sessão) não pôde ser validado nesta sessão por uma dessincronia pré-existente do histórico de migrations do banco compartilhado, não relacionada a esta implementação — ver `.ai/CURRENT_SPRINT.md`. Perfis/Permissões (catálogo global, ratificados nesta sprint) e Bootstrap completo permanecem não implementados, apenas com modelo de dados preparado.
- **O1.4.2 — Login Passwordless OTP e Sessão Segura: ✅ CONCLUÍDA (07/08/2026).** Security Implementation Gate III: **APROVADO COM PENDÊNCIAS NÃO BLOQUEANTES PARA DEVELOPMENT**, cobrindo O1.4.2 + as micro-iterações O1.4.2.1 (Security Hardening) e O1.4.2.2 (Hardening do `DevelopmentHeaderAuthenticationHandler`). Satisfaz integralmente ADR-0020, item 13 (revisão antes + validação depois). Pendências carregadas para o Authentication Infra Readiness Gate (antes de Homologação, não bloqueantes agora): validação de RowVersion+índice único filtrado em SQL Server real; CSP efetiva da SPA; `EmailAuditHasher`/HMAC; sincronização de migrations do banco compartilhado; provedor corporativo/Entra ID/Graph. Detalhe: `.ai/CURRENT_SPRINT.md`.
- **O1.4.3 — Security Design do Bootstrap e Administrador Sênior: ✅ concluída (07/08/2026).** Revisão arquitetural de segurança do Bootstrap Mode (ADR-0020, item 12) exigida antes de qualquer implementação, sem nenhum código/endpoint/migration implementado. **Bootstrap Security Design Gate: APROVADO COM PENDÊNCIAS** (nenhuma bloqueante — apenas decisões de detalhamento de implementação). Decisões principais: dois estados (`Concluido` false/true, não quatro); identidade inicial por lista explícita de e-mails pré-autorizados; sessão Bootstrap distinta e de privilégios limitados; conclusão transacional com `UPDATE` condicional atômico; permite múltiplos Administradores Sênior; proíbe ficar sem nenhum ativo; Bootstrap nunca reabre; Recovery é procedimento operacional separado, offline. Documento: [security-design-auth-o1.4.md](../docs/architecture/security-design-auth-o1.4.md), seção 20. **Próxima frente candidata:** Work Order técnica de implementação da O1.4.3, condicionada à ratificação do Product Owner/CTO.
- **Work Order Técnica O1.4.3 — Bootstrap Mode e Administrador Sênior: planejamento técnico concluído em 07/08/2026** (registro histórico preservado abaixo). Transforma o Security Design (seção 20 de `security-design-auth-o1.4.md`) em plano executável: fluxo completo, sessão Bootstrap distinta (`BootstrapSessao`/`BootstrapSession`/`BootstrapAuthenticated`), Bootstrap Secret/allowlist, hardening de `BootstrapEstado`, regra do último Administrador Sênior, API e frontend propostos, 2 migrations previstas (na época, não criadas), 22 categorias de teste. 5 divergências encontradas entre Security Design/documentos de produto/código real, nenhuma bloqueante (detalhe: `.ai/CURRENT_SPRINT.md`). Recomendou divisão em 4 etapas (O1.4.3.1–O1.4.3.4). **Estado atual (10/08/2026): a implementação das 4 etapas foi concluída integralmente e a O1.4.3 está FORMALMENTE CONCLUÍDA** — ver registro abaixo ("O1.4.3 — Bootstrap e Administrador Sênior: FORMALMENTE CONCLUÍDA (10/08/2026)"). Work Order: [O1.4.3](work-orders/completed/O1.4.3-BootstrapEAdministradorSenior.md) (movida de `active/` para `completed/` no fechamento formal).
- **Ato documental — Autorização explícita do Product Owner/CTO para início da O1.4.3.1 (07/08/2026):** o Product Owner/CTO (Julio) confirmou explicitamente, em 07/08/2026, a aprovação do início da implementação da Etapa O1.4.3.1 — Fundação Backend do Bootstrap (`BootstrapEstado`, `BootstrapSessao`, `BootstrapSessionAuthenticationHandler` + política `BootstrapAuthenticated`, endpoints `GET /bootstrap/estado`/`POST /bootstrap/iniciar`/`POST /bootstrap/otp/verificar`, sem `/bootstrap/concluir`), conforme escopo já definido na seção 21 da Work Order O1.4.3. Esta aprovação não estava registrada nos documentos canônicos até este momento; este registro é exclusivamente o ato documental da autorização, feito antes do início da implementação. A implementação da O1.4.3.1 está iniciando; a O1.4.3 como um todo **permanece não concluída**, e nenhuma etapa é declarada "Pronta" apenas por esta autorização — build/testes aprovados, Security Self-Review e, ao final da O1.4.3 completa, Security Validation independente continuam exigidos (ADR-0020, item 13). Commit/push: não realizado por este ato documental.
- **O1.4.3.1 — Fundação Backend do Bootstrap: implementação técnica PARCIAL, NÃO concluída (07/08/2026).** Código de produção e testes escritos integralmente conforme a Work Order O1.4.3 (seção 21) e o Security Design (§20): `BootstrapEstado`/`BootstrapSessao` (Domain), `IBootstrapEstadoRepository`/`IBootstrapSessaoRepository`/`BootstrapSecretOptions`/`BootstrapAllowedCandidatesOptions`/casos de uso (Application), repositórios EF/`BootstrapSecretOptionsValidator`/configurações EF incl. índice único de `Perfil` por (`UnidadeNegocioId`,`Nome`) (Infrastructure), `BootstrapSessionAuthenticationHandler`+política `BootstrapAuthenticated`+`BootstrapController` com os 3 endpoints (`estado`/`iniciar`/`otp/verificar`, sem `concluir`) (Api), 2 migrations hand-escritas (`AddBootstrapEstado`, `AddPerfilNomeUnidadeNegocioUniqueIndex`) e suíte de testes cobrindo o subconjunto do plano de testes (seção 18) atribuído a esta etapa. **Bloqueio ambiental confirmado e documentado:** nenhum `dotnet restore`/`build`/`test`/`ef migrations add` pôde ser executado nesta sessão — o ambiente de shell disponível não tem .NET SDK instalado, sem rede (allowlist bloqueia downloads) e sem privilégios de root para instalá-lo. **Por isso a etapa NÃO é declarada concluída** — falta confirmar build/testes verdes (pré-condição obrigatória, nunca suficiente isoladamente, Work Order seção 19/24) antes de qualquer avanço para O1.4.3.2. Nenhuma migration foi aplicada em nenhum banco. Nenhum arquivo de frontend foi tocado. Detalhe completo: `.ai/CURRENT_SPRINT.md`.
- **O1.4.3.1 — Fundação Backend do Bootstrap: ✅ CONCLUÍDA (07/08/2026).** Fecha formalmente o bloqueio ambiental do registro anterior. Validação real executada no Mac do Product Owner (.NET SDK 9.0.316) em duas sessões de correção: (1) 7 testes falhos (nunca visíveis no ambiente sem SDK) corrigidos exclusivamente no harness de teste — DbContext InMemory de teste para satisfazer `ValidateOnBuild` do host, `Bootstrap:Secret` configurado nos testes que o exigem, `PerfilUniqueIndexTests` reescrito para validar a declaração do índice via metadata do EF (o provider InMemory não aplica constraints relacionais) — nenhuma regra de segurança alterada; (2) duplicação de `CREATE TABLE [Fornecedores]` na cadeia de migrations, causada pela ausência histórica de um `BlueprintOSDbContextModelSnapshot.cs` commitado (as 8 migrations de Fornecedor foram sempre manuscritas, nunca geradas por `dotnet ef`), corrigida via migration `BaselineFornecedorSnapshot` (NO-OP) + regeneração fiel das 4 migrations de Identity/Bootstrap via `dotnet ef migrations add` real. **Evidências finais:** build limpo; **369/369 testes aprovados**; `has-pending-model-changes` → sem mudanças pendentes; `migrations script 0` → exatamente 1 `CREATE TABLE [Fornecedores]` (linha 12, histórica), zero depois do baseline. **Migrations Identity/Bootstrap: VALIDADAS, MAS NÃO APLICADAS** — nenhum `database update` foi executado; o banco compartilhado permanece com migrations aplicadas somente até `202608020001_B213FornecedorErpSyncHardening`, inalterado. Aplicação ao banco compartilhado é decisão operacional separada, ainda pendente. A O1.4.3 (etapa-mãe) **permanece não concluída** — restam O1.4.3.2/.3/.4 e a Security Validation independente final; **O1.4.3.2 não iniciada**. Commit/push: não realizado. Detalhe completo: `.ai/CURRENT_SPRINT.md`.
- **O1.4.3.2 — Conclusão Transacional e Administrador Sênior: ✅ CONCLUÍDA (10/08/2026).** `ConcluirBootstrapUseCase` implementa a conclusão transacional (transação implícita de um único `SaveChangesAsync`): cria/reaproveita `UnidadeNegocio`, cria o `Usuario` Administrador Sênior (e-mail exclusivamente da `BootstrapSessao` validada por OTP, nunca do payload), cria/reaproveita o `Perfil` "Administrador Sênior" pelo índice único, vincula `UsuarioPerfil`, invoca a invariante do último Administrador Sênior ativo, e conclui `BootstrapEstado` via compare-and-swap otimista (`RowVersion`, migration `20260810120746_AddBootstrapConclusaoConcurrency`, contendo exclusivamente essa coluna). Endpoint `POST /bootstrap/concluir` sob `BootstrapAuthenticated`, CSRF e rate limiting próprios. **388/388 testes aprovados** (incluindo concorrência real via EF Core InMemory — exatamente uma conclusão vence, nenhuma entidade órfã). Migration gerada via `dotnet ef` real, auditada (única alteração de modelo: `RowVersion` em `BootstrapEstado`), **validada mas não aplicada** ao banco compartilhado (inalterado); nenhuma migration histórica/reconciliada alterada. A Work Order mãe O1.4.3 **permanece ativa** — restam O1.4.3.3 (Frontend Bootstrap, não iniciada) e O1.4.3.4 (Security Self-Review dedicada/preparação da Security Validation independente, não iniciada), esta última exigida antes de O1.4.3 ser "Pronta" (ADR-0020, item 13). Commit/push: não realizados. Detalhe completo: `.ai/CURRENT_SPRINT.md`.
- **O1.4.3.3 — Frontend Bootstrap: ✅ CONCLUÍDA (10/08/2026).** Vertical Slice `frontend/web/src/bootstrap/` (mesmo padrão físico de `auth/`): `bootstrapApi.ts`, `useBootstrapEstado`, `BootstrapGate` (decisão de roteamento raiz `/bootstrap` vs. `/login`/área autenticada, exclusivamente UX), `BootstrapRoutes`, `BootstrapPage` (wizard completo: passo 0 de segurança — e-mail + Bootstrap Secret + OTP — seguido de Unidade de Negócio, Administrador Sênior sem campo de e-mail, confirmação explícita), `tests/BootstrapPage.test.tsx` (15 testes). `frontend/web/src/core/AppRoutes.tsx` ajustado para incluir `/bootstrap/*` sem alterar rotas existentes. Nenhum e-mail é aceito/enviado no payload de `/bootstrap/concluir` (confirmado contra o `BootstrapConcluirRequest` real do backend). Suíte de frontend **53/53 aprovada** (sem regressão); `tsc -b`/`vite build` limpos. **Encerramento formal nesta sessão** após smoke test real completo em Chrome (via Chrome DevTools MCP), executado e aprovado pelo Product Owner/CTO: fluxo ponta a ponta (acesso → e-mail autorizado → Bootstrap Secret → OTP → sessão de Bootstrap com cookie `mc_bootstrap_sid` autenticado corretamente pelo backend → Unidade de Negócio "Grupo Soma" (slug `grupo-soma`) → Administrador Sênior "Julio Cesar" (e-mail julio.cesar@somagrupo.com.br) → confirmação → `POST /bootstrap/concluir` → 200 OK); `GET /bootstrap/estado` passou de `{"disponivel":true}` para `{"disponivel":false}` (`BootstrapEstado.Concluido = true`), conclusão real, intencional e autorizada, não revertida. Um 401 relatado em investigação anterior **não foi reproduzido** neste smoke test; a hipótese de falha por cookie `Secure` em loopback foi refutada por teste real; nenhuma causa definitiva do 401 anterior foi comprovada e nenhuma correção foi criada para um problema não reproduzido. A O1.4.3.1 e a O1.4.3.2 permanecem CONCLUÍDAS e não foram reabertas; a O1.4.3.4 (Security Self-Review dedicada + preparação da Security Validation independente) permanece **NÃO INICIADA**, deliberadamente fora do escopo desta sessão. A Work Order mãe O1.4.3 permanece ATIVA. Commit/push: não realizados. Detalhe completo: `.ai/CURRENT_SPRINT.md`.
- **O1.4.3.4 — Security Self-Review dedicada + preparação da Security Validation independente: ✅ CONCLUÍDA (10/08/2026).** Revisão adversarial dedicada do código real (4 auditorias paralelas independentes cobrindo Bootstrap Secret/allowlist/OTP, BootstrapSession/CSRF/autorização, conclusão transacional/concorrência/Administrador Sênior, e superfícies exclusivas de Development), cruzando Security Design (§20) → Work Order → código → testes → configuração → migrations → comportamento real já observado no smoke test da O1.4.3.3. **Nenhum CRITICAL/HIGH confirmado** (um HIGH condicional sobre índice único de `UnidadeNegocio.Slug` foi verificado e refutado — `HasIndex(x => x.Slug).IsUnique()` confirmado em `UnidadeNegocioConfiguration.cs`). Findings reais remanescentes: 3 MEDIUM (detecção de violação de índice único por substring de mensagem de exceção, em vez de código de erro estruturado; corrida sobre índice único de `Perfil` no cenário de reaproveitamento de BU existente não exercitada por teste de concorrência real; invariante do último Administrador Sênior chamada com valor hardcoded, sem risco nesta etapa mas ponto de atenção para fluxos futuros de inativação) e 2 LOW (`BootstrapSessao` não revogada em falhas de payload de `/bootstrap/concluir`; `DevelopmentRequestIdentity` sem checagem própria de loopback, assimétrica em relação aos demais componentes de Development) — todos apresentados como diagnóstico, nenhuma correção aplicada silenciosamente. Validação completa: `dotnet build`/`dotnet test` **388/388** sem regressão; `tsc`/`vite build`/`vitest` **53/53**; `has-pending-model-changes` sem alterações pendentes; `migrations script 0` com exatamente 1 `CREATE TABLE [Fornecedores]` histórico; `git diff --check` limpo. Nenhuma migration aplicada, nenhum commit/push realizado. Handoff para Security Validation independente preparado (escopo + evidências) em `.ai/CURRENT_SPRINT.md`. **A Work Order mãe O1.4.3 permanece ATIVA — estado atualizado para AGUARDANDO SECURITY VALIDATION INDEPENDENTE**, não movida para `completed/`. Detalhe completo: `.ai/CURRENT_SPRINT.md`.
- **O1.4.3 — Bootstrap e Administrador Sênior: FORMALMENTE CONCLUÍDA (10/08/2026).** Security Validation independente executada por revisor logicamente isolado (sem acesso prévio aos achados da Self-Review O1.4.3.4 acima), cobrindo as 13 áreas obrigatórias de escopo com provas adversariais próprias — **resultado: 0 CRITICAL, 0 HIGH**; parecer **APROVADA COM RESSALVAS**. Duas divergências entre a Self-Review e a Validação independente (loopback de `DevelopmentRequestIdentity`; detecção de índice único por mensagem de exceção) foram reconciliadas por leitura direta adicional do código — nenhum achado subiu de severidade; ambos desceram (LOW→INFORMATIONAL; MEDIUM→LOW). **O Product Owner aceitou formalmente as ressalvas em 10/08/2026** — 15 findings remanescentes (4 MEDIUM + 6 LOW + 5 INFORMATIONAL) registrados em `.ai/BACKLOG.md`, nenhum bloqueante para esta Work Order; MEDIUM 1 (`Cache-Control: no-store` em `/bootstrap/*`) e MEDIUM 2 (entropia do Bootstrap Secret) **bloqueiam explicitamente a promoção para Homologação**. A O1.4.3 está **FORMALMENTE CONCLUÍDA**; Work Order movida para `.ai/work-orders/completed/O1.4.3-BootstrapEAdministradorSenior.md`. Build/testes/migrations reconfirmados sem regressão nesta sessão de fechamento (388/388 backend, 53/53 frontend, `has-pending-model-changes` limpo, `git diff --check` limpo). Nenhuma migration aplicada, nenhum commit/push realizado. Detalhe completo: `.ai/CURRENT_SPRINT.md`.
- **Correção pontual pós-O1.4.3 — Regressão de autenticação em Development (10/08/2026):** ao validar o login normal pela primeira vez após o encerramento formal da O1.4.3, identificadas e corrigidas duas falhas de fundação (não RBAC, não Bootstrap): (A) o esquema default de autenticação em Development ignorava sessões OTP reais via cookie `mc_sid`, atendendo somente ao header `X-Development-User-Id` — corrigido com `PolicyScheme` em `Program.cs` (cookie presente → `SessionCookieAuthenticationHandler`; ausente → `DevelopmentHeaderAuthenticationHandler`; cookie inválido nunca cai para o header); (B) `ICurrentIdentity` em Development (`DevelopmentRequestIdentity`) reparseava o header diretamente, ignorando `HttpContext.User` já autenticado — corrigido trocando para `SessionCurrentIdentity` (mesma classe de produção). Validado ao vivo no Chrome: OTP → `/auth/me` 200 → Dashboard → navegação estável entre rotas protegidas. 10 testes novos (`DevelopmentSessionCookiePipelineTests.cs`, `DevelopmentCurrentIdentityPipelineTests.cs`); backend 393 testes unitários + 5 integração aprovados, frontend 53/53, `git diff --check` limpo. Achado separado registrado como pendência (não corrigido): `/fornecedores?q=...` retorna 500 por drift de schema (`Invalid column name 'Cnpj'/'Nome'`), sem relação com identidade — ver `.ai/BACKLOG.md`. **A O1.4.3 não foi reaberta; permanece FORMALMENTE CONCLUÍDA. A O1.5 permanece NÃO INICIADA.** Detalhe completo: `.ai/CURRENT_SPRINT.md`.
- **O1.4.1.1 — Formalização da Estratégia de Autenticação em Development: ✅ concluída (07/08/2026).** Revisão adversarial da estratégia de desenvolvimento do fluxo OTP sem dependência imediata da Infra; **Development Auth Strategy aprovada com ajustes pelo Product Owner**. Formaliza: fase Frontend First sem solicitação à Infra agora; contrato `IOtpEmailSender` exclusivo para OTP; `DevelopmentOtpEmailSender` restrito a Development, sem fallback e com fail-closed fora dele; defesa em profundidade por `IHostEnvironment` + `ValidateOnStart()`; proibição absoluta de OTP em log/API/UI/arquivo/telemetria/auditoria inclusive em Development; Authentication Infra Readiness Gate obrigatório antes de Homologação; Entra ID/Microsoft Graph postergados, não descartados. Documento: [security-design-auth-o1.4.md](../docs/architecture/security-design-auth-o1.4.md), seção 17. **Efeito:** o bloqueador nº 2 do Security Design Gate (provedor de e-mail) deixa de impedir o início de O1.4.2, passando a ser exigido apenas antes de Homologação; bloqueadores nº 1, 3 e 4 e a ratificação do modelo de sessão permanecem pendentes de decisão do Product Owner/CTO.

## Revisão Arquitetural R2 — Arquitetura Frontend (06/08/2026) — concluída

- **Status:** a **Revisão Arquitetural R2 está concluída**. **Arquitetura Frontend aprovada**: **Vertical Slice aprovado** como padrão obrigatório para toda a aplicação React do +Compras. O **Frontend está liberado para implementação** sob esse padrão a partir da Sprint O1.2.
- **Decisão aceita:** [ADR-0020](./DECISIONS.md), seção "Arquitetura Frontend" (acrescentada pela R2) — o frontend organiza-se por domínio de negócio, nunca por tipo técnico; cada módulo de domínio (Vertical Slice) possui autonomia funcional, agrupando internamente `pages`, `components`, `hooks`, `services`, `routes`, `models`, `types` e `tests`; elementos genuinamente compartilhados residem exclusivamente em `shared`/`design-system`; Frontend e Backend compartilham a mesma visão arquitetural de organização por domínio (Backend: Modular Monolith + Clean Architecture + DDD pragmático, ADR-0001). Estrutura conceitual de referência registrada na ADR (`core/`, `authentication/`, `administration/{usuarios,perfis,filiais,centros-custo,unidades-alocacao}/`, `procurement/`, `workflow/`, `shared/`, `design-system/`) — conceitual, não obrigatória em nomes exatos, podendo evoluir sem quebrar os princípios.
- **Natureza da revisão:** exclusivamente arquitetural/documental — nenhuma estrutura de pastas foi criada, nenhum código de frontend ou backend foi alterado, e nenhuma migration foi criada. Documentos atualizados: `.ai/DECISIONS.md` (ADR-0020), `docs/architecture/domain-principles.md` (nova seção "Frontend"), `.ai/ENGINEERING_BLUEPRINT.md` (nova subseção "Arquitetura Frontend — Vertical Slice").
- **Impacto:** esta decisão impacta toda a implementação da Onda 1 — a Work Order de Estrutura da O1.2 deve aplicar o padrão Vertical Slice desde o início da implementação do frontend, incluindo a migração conceitual do código React já existente do módulo Fornecedores para essa organização (pendência registrada na ADR).
- **Pendência:** a estrutura conceitual de exemplo ainda não foi validada contra o código React já existente do Portal +Compras; a definição de como esse código migra para o padrão Vertical Slice (ou nasce direto na nova estrutura) é responsabilidade da Work Order de Estrutura da O1.2.
- **Dashboard:** esta revisão não executa o comando `[atualizar dashboard]`; `DASHBOARD_STATE.md` não foi alterado.

## Sprint O1.2.1 — Fundação Física do Frontend Vertical Slice (06/08/2026) — ✅ concluída

- **Status:** ✅ concluída e encerrada oficialmente. **Frontend Vertical Slice implantado**: estrutura física criada em `frontend/web/src` (`core/`, `procurement/suppliers/`, `shared/components/`) e **primeira Slice oficial** (`procurement/suppliers`) criada a partir da migração do módulo Fornecedores, servindo de referência para os próximos módulos/domínios. Sem alteração de comportamento funcional, regra de negócio, contrato de API, endpoint ou UI — migração exclusivamente arquitetural. Work Order registrada em `.ai/work-orders/completed/O1.2.1-FundacaoFisicaFrontendVerticalSlice.md`; catalogada em `.ai/BACKLOG.md`.
- **Estrutura criada:** `core/` (AppShell, AppRoutes — infraestrutura transversal) e `shared/components/` (StatusBadge, único artefato comprovadamente usado por mais de um domínio hoje). `pages/` e `components/` deixam de conter qualquer artefato de Fornecedores; nenhuma implementação duplicada remanescente.
- **Validações:** `npx tsc -b` (0 erros), `npm run build` (aprovado), `npm run test` (4/4 testes de `CadastroFornecedor` aprovados, sem alteração de asserções), smoke test do `npm run dev` (AppShell e rota `/fornecedores` carregam e resolvem módulos JS sem erro de import; erro 500 observado é o proxy do Vite para a API em `:8080`, backend não estava rodando — pré-existente).
- **Pendência resolvida da R2:** a pendência registrada acima ("a definição de como esse código migra para o padrão Vertical Slice ... é responsabilidade da Work Order de Estrutura da O1.2") foi endereçada por esta sprint.
- **Pendências novas registradas** (detalhe completo em `.ai/CURRENT_SPRINT.md`): demais páginas demonstrativas (`Dashboard`, `Pedidos`, `Negociações`, `Indicadores`, `Agentes IA`, `Configurações`) ainda não migradas para slices próprias; pasta `design-system/` da estrutura conceitual não criada (hoje o Design System é só `@import` CSS cross-repo, sem código para mover); divergência de porta `.env.example` (5262) vs. proxy Vite (8080) identificada mas não corrigida; sem aliases de import configurados.
- **Dashboard:** esta sprint não executa o comando `[atualizar dashboard]`; `DASHBOARD_STATE.md` não foi alterado. Quando executado, o comando deverá refletir a conclusão da Sprint O1.2.1.
- **Próxima sprint:** **O1.2.2**, para aplicar o padrão consolidado nesta sprint aos demais módulos/domínios ainda pendentes e às telas de Administração da Onda 1. Registrada em `.ai/BACKLOG.md`; abertura formal depende de autorização explícita.

## Divergências ainda abertas

- O roadmap estratégico de apresentação +COMPRAS continua sem atualização visual; as correções necessárias estão listadas em `resources/presentations/ROADMAP_UPDATE.md`.
- A estrutura alvo descrita em `ARCHITECTURE.md` não é a estrutura física atual.
- Nenhuma Work Order futura está aprovada; a próxima sprint depende de decisão explícita do Product Owner.
- ~~**Módulo `administration/users` sem Work Order/sprint própria documentada**~~ — **regularizada em 06/08/2026** (Housekeeping Administrativo): Work Order retroativa criada em `.ai/work-orders/completed/O1.3.2-GestaoDeUsuarios.md`, com base exclusivamente nas evidências já existentes (código e 3 testes pré-existentes de `UsuariosPage.test.tsx`). A lacuna na sequência documentada (O1.3.1 — Perfis — para O1.3.3 — Filiais) permanece sem Work Order equivalente para O1.3.1; fora do escopo desta regularização.

## Consolidação e Plano Executável de Conclusão da Onda 1 (10/08/2026)

- **Natureza:** sessão exclusivamente documental — nenhum código, migration, frontend ou backend foi alterado; nenhuma nova sprint foi iniciada; nenhuma Work Order foi ativada; nenhum commit/push foi realizado.
- **Decisões do Product Owner:** ADR-0021 (`.ai/DECISIONS.md`) registra formalmente D1 (Administração funcional real), D2 (RBAC real), D3 (Filiais/Centros de Custo integrados ao ERP), D4 (Centro de Custo × Unidade de Alocação N:N obrigatório), D5 (O1.2.2 pertence à Onda 1), D6 (`design-system/` não é requisito isolado), D7 (blueprint administrativo evolui com a implementação real), D8 (`PortalMaisComprasFrontend.md` superseded).
- **Reconciliação:** os 41 entregáveis oficiais da Onda 1 foram auditados individualmente contra o dashboard oficial (`.ai/dashboard/DASHBOARD_STATE.md`); nenhum foi retirado, absorvido ou substituído por falta de evidência canônica suficiente. Métrica oficial inalterada: 41 entregáveis / 7 Concluído / 11 Em desenvolvimento / 23 Planejado / 17% de Progresso Técnico. Matriz completa e plano executável em [`docs/audits/Onda1-Reconciliacao-e-Plano-Execucao.md`](../docs/audits/Onda1-Reconciliacao-e-Plano-Execucao.md).
- **Ressalva registrada (não decidida nesta sessão):** os entregáveis "+Compras Funcional", "+Compras UX" e "+Compras Data Model" (#33–#35) permanecem "Concluído" nesta sessão, mas a própria observação do Dashboard já registra que representam apenas estrutura documental inicial, não a especificação completa por tela — decisão de manter ou reclassificar esse status cabe ao Product Owner, não foi tomada aqui.
- **Novas Work Orders propostas (todas Draft/Planejada, nenhuma Approved/Active):** O1.5 (RBAC Real) a O1.14 (Blueprint de Banco e Validação Final) — catalogadas em `.ai/BACKLOG.md`, arquivos em `.ai/work-orders/backlog/`. A ativação da primeira (O1.5) depende de autorização explícita do Product Owner.
- **Gates de Homologação preservados:** Cache-Control `no-store` em `/bootstrap/*`, validação de entropia do Bootstrap Secret e provedor corporativo de e-mail OTP continuam bloqueando apenas a promoção para Homologação — não reabertos, não corrigidos nesta sessão.
- **Destino da Work Order `PortalMaisComprasFrontend.md`:** movida de `.ai/work-orders/active/` para o novo diretório canônico `.ai/work-orders/superseded/` (criado por esta sessão e documentado em `.ai/work-orders/README.md`), com nota de cabeçalho registrando a ADR-0021 como origem da decisão D8.

## Sprint O1.5 — RBAC Real (11/08/2026) — ✅ FORMALMENTE CONCLUÍDA

- **Status:** ✅ **FORMALMENTE CONCLUÍDA (11/08/2026).** Implementação concluída; Security Validation independente **executada** (veredito APROVADA COM RESSALVAS: 0 CRITICAL, 0 HIGH após correções); **ressalvas remanescentes aceitas formalmente pelo Product Owner (Julio Cesar) em 11/08/2026**, exatamente o precedente da O1.4.3. Work Order movida para `.ai/work-orders/completed/O1.5-RbacReal.md`. O aceite encerra a Work Order, **não** as pendências — as ressalvas seguem abertas e rastreadas em `.ai/BACKLOG.md`. Este aceite **não** autoriza promoção a Homologação.
- **O que passou a existir de fato:** o ciclo RBAC fecha ponta a ponta — Perfil → Permissões → Usuário×Perfil → identidade autenticada → policies do ASP.NET Core → enforcement real (401/403/200). `PermissaoCatalogo` é a fonte central única dos 14 códigos de permissão (derivados exclusivamente dos já documentados em `ComprasFuncional.md`); as policies, o seed de banco, os endpoints protegidos e o catálogo do frontend derivam todos dele. Permissões efetivas = união dos Perfis **ativos** da Unidade de Negócio da sessão, **reresolvida a cada requisição** (revogação imediata).
- **Mock removido:** `perfisMockApi.ts` e o catálogo estático de permissões do frontend foram **excluídos do repositório**; `administration/profiles` consome a API real, com padrão visual aprovado preservado.
- **Migration:** `20260811143355_AddRbacPerfilPermissaoCatalogo` — colunas novas em `Perfis`, seed das 14 permissões com Ids estáveis, FKs `RESTRICT` em `PerfisPermissoes`/`UsuariosPerfis`, e dois blocos de SQL manual documentados (backfill de timestamps; concessão idempotente do catálogo aos Perfis "Administrador Sênior" existentes, sem a qual o único administrador do ambiente ficaria com 403 permanente). **Aplicada** ao banco de desenvolvimento `MaisCompras`.
- **Validações:** `dotnet build` 0 erros/0 avisos; `dotnet test` **477 aprovados** (472 unitários + 5 integração; baseline 398); `tsc -b`/`vite build` limpos; Vitest **61 aprovados** (baseline 53); `has-pending-model-changes` limpo; `migrations script 0` com exatamente 1 `CREATE TABLE [Fornecedores]` histórico. Smoke test real em Chrome + API + SQL Server reais: 401/403/200 reais, CRUD persistido, invariante em 409, catálogo de 14 permissões vindo da API, persistência após reload, zero erros de console.
- **Correções de segurança aplicadas após a validação independente:** escalonamento de privilégio ilimitado por `Perfil.Gerenciar` (HIGH), permissões efetivas não escopadas por Unidade de Negócio (MEDIUM), invariante anti-auto-bloqueio satisfeita por Perfil com zero usuários (MEDIUM) e CSRF opt-in por rota (LOW) — cada uma com teste de regressão.
- **Ressalvas remanescentes (não corrigidas, para decisão do Product Owner):** checagem de invariante não serializada com a escrita (MEDIUM); backfill do catálogo por nome de Perfil em vez do Id do `BootstrapEstado` (LOW — não corrigido deliberadamente, pois editar migration já aplicada recriaria o drift histórico reconciliado na O1.4.3.1); ausência de auditoria de alterações de RBAC (LOW); ausência de rate limiting no grupo administrativo (LOW); `ClaimTypes.Role` residual, `RequestIdentity.Permissoes` não lido por nenhum caso de uso, testes de pipeline em rotas `/probe-*` sintéticas (INFORMATIONAL).
- **D2 (ADR-0021) satisfeita apenas parcialmente:** `Fornecedor.*` e `Pedido.*` existem no catálogo, mas os endpoints de Fornecedores/Negociações seguem protegidos apenas por autenticação. Aplicar RBAC a eles é mudança de comportamento em módulos fora do escopo declarado da O1.5.
- **Progresso técnico da Onda 1 recalculado no fechamento formal (11/08/2026): 17% → 20%** (19,51% exato). Regra-fonte: `.ai/dashboard/DASHBOARD_STATE.md`, "Política dos percentuais" — só entregável "Concluído" conta; "Em desenvolvimento" sem percentual individual contribui 0, nunca estimado. Com o aceite das ressalvas, a **única** condição registrada que mantinha o entregável **#17 "Perfis, papéis e permissões"** em "Em desenvolvimento" foi satisfeita → **Concluído**; o entregável **#9 "Perfis de usuário simulados"** passou de "Planejado" para "Em desenvolvimento", aplicando a reclassificação já recomendada por escrito no `DASHBOARD_STATE.md` (contribui 0, sem percentual individual). Resultado: 41 entregáveis / **8 Concluído / 11 Em desenvolvimento / 22 Planejado**; Contribuição da Onda 1 ao MVP 3,4 → **3,9 pontos**; Percentual Global do MVP 1.0 30,41% → **30,90%** (exibido 31%). Durante a execução da sprint o percentual foi deliberadamente mantido em 17%; nenhum percentual foi inflado por abertura ou por implementação de sprint.
- **Dados de teste mantidos no banco de desenvolvimento (decisão do Product Owner, 11/08/2026):** os **quatro** Perfis **inativos** deixados pelo smoke test (`Analista (O1.5 smoke)`, `Aprovador (smoke UI)`, `Pos-hardening`, `Verificacao pos-hardening`) **permanecem** no banco `MaisCompras` de desenvolvimento como dados técnicos reutilizáveis. Não removidos; nenhuma exclusão física criada; nenhuma migration de limpeza criada. O saneamento pertence a uma atividade futura **anterior à promoção para HOMOLOGAÇÃO/REVIEW**, fora do escopo da O1.5.
- **Progresso técnico da Onda 1 recalculado no fechamento formal da O1.6 (11/08/2026): 20% → 24%** (24,3902% exato = 10 ÷ 41). Regra-fonte inalterada (só "Concluído" conta). Os entregáveis **#15 "Usuários"** e **#16 "Usuário por Unidade de Negócio"** passam para **Concluído** — mock de `administration/users` substituído por backend/persistência reais, com `Usuario.UnidadeNegocioId` como escopo obrigatório de toda leitura/escrita. Resultado: 41 entregáveis / **10 Concluído / 10 Em desenvolvimento / 21 Planejado**; Contribuição da Onda 1 ao MVP 3,9 → **4,9 pontos**; Percentual Global do MVP 1.0 30,90% → **31,90%** exato (exibido 32%). **Por instrução expressa do Product Owner nesta sessão, `.ai/dashboard/DASHBOARD_STATE.md` não foi editado** — este recálculo é registrado apenas em `PROJECT_STATE.md`/`CURRENT_SPRINT.md`/`BACKLOG.md`; a atualização do Dashboard oficial (rotina `[atualizar dashboard]`) permanece com o Product Owner. Usuária de teste "Maria Teste O1.6" (Ativa, criada pelo smoke test real) permanece no banco de desenvolvimento, mesmo precedente da O1.5.
- **Pendências de produto seguem pendentes:** catálogo definitivo de Perfis de negócio e nomenclatura de `CentroCusto.Acessar` — não decididos nem implementados neste fechamento.
- **Progresso técnico da Onda 1 recalculado no fechamento formal da O1.7 (11/08/2026): 24% → 29%** (29,2683% exato = 12 ÷ 41). Os entregáveis **#14 "Empresas e filiais"** e **#18 "Centros de Custo"** passam para **Concluído**. Resultado: 41 entregáveis / **12 Concluído / 8 Em desenvolvimento / 21 Planejado**; Contribuição da Onda 1 ao MVP 4,9 → **5,85 pontos**; Percentual Global do MVP 1.0 31,90% → **32,85%** exato (exibido 33%).
- **Progresso técnico da Onda 1 recalculado no fechamento formal da O1.8 (11/08/2026): 29% → 32%** (31,7073% exato = 13 ÷ 41). Regra-fonte inalterada (só "Concluído" conta). O entregável **#19 "Unidades de Alocação"** passa para **Concluído** — mock de `administration/allocation-units` substituído por backend/persistência reais (`Domain/Application/Infrastructure/Api.Identity`, mesmo padrão de `Usuario`/O1.6), sem vínculo com Centro de Custo (escopo da O1.9) e sem integração ERP (ADR-0020, item 4). Resultado: 41 entregáveis / **13 Concluído / 7 Em desenvolvimento / 21 Planejado**; Contribuição da Onda 1 ao MVP 5,85 → **6,34 pontos**; Percentual Global do MVP 1.0 32,85% → **33,34%** exato (exibido 33%). **Por instrução expressa do Product Owner, `.ai/dashboard/DASHBOARD_STATE.md` não foi editado** — este recálculo é registrado apenas em `PROJECT_STATE.md`/`BACKLOG.md`; a atualização do Dashboard oficial (rotina `[atualizar dashboard]`) permanece com o Product Owner.
- **Progresso técnico da Onda 1 — inalterado no fechamento formal da O1.9 (11/08/2026): permanece 32%** (13 ÷ 41 = 31,7073% exato). O relacionamento N:N Centro de Custo × Unidade de Alocação (D4/ADR-0021) não é um entregável isolado da lista de 41 — é condição de fechamento pleno dos entregáveis **#18** e **#19**, ambos já Concluídos (O1.7/O1.8). Nenhum entregável foi criado, retirado, absorvido ou substituído por esta sprint. MVP Global permanece 33,34% exato, exibido 33%.
- **Progresso técnico da Onda 1 — inalterado no fechamento formal da O1.10 (11/08/2026): permanece 32%** (13 ÷ 41 = 31,7073% exato). Migração estrutural pura e reflexo real de RBAC no menu não substituem a implementação de domínio real dos módulos ainda demonstrativos (Negociações, Pedidos, Indicadores, Agentes IA, Configurações) — nenhum dos entregáveis **#4–#8** passa a "Concluído". MVP Global permanece 33,34% exato, exibido 33%.
- **Divergência documental corrigida:** o registro de que as migrations de Identity/Bootstrap estariam "validadas mas não aplicadas" ao banco compartilhado está **desatualizado** — `dotnet ef migrations list` contra o banco real mostra todas aplicadas até `20260810120746_AddBootstrapConclusaoConcurrency`, coerente com o Bootstrap concluído no smoke test da O1.4.3.3.
- **Documentação:** [rbac-o1.5.md](../docs/architecture/rbac-o1.5.md) (novo) e a fatia administrativa de Identidade/RBAC em [Database.md](../docs/database/Database.md) (blueprint incremental, D7). Nenhuma ADR nova: a mecânica já estava decidida pela ADR-0020.
- **Commit/push:** os três commits de implementação (`c98b916`, `1ad23c3`, `8c4b10b`) mais um commit dedicado de fechamento documental foram **publicados em `origin/main`** em 11/08/2026, com **autorização expressa do Product Owner**, via `git push origin main` — sem force push e sem reescrita de histórico.
- **O1.6 NÃO iniciada** — permanece em `.ai/work-orders/backlog/O1.6-GestaoDeUsuariosBackendReal.md`, Draft/Planejada; não movida, não aberta, não implementada. É apenas a próxima candidata do caminho crítico.
- **Nenhuma Work Order ativa** ao final deste fechamento: `.ai/work-orders/active/` está vazio (somente `.gitkeep`). A próxima frente depende de decisão e autorização explícitas do Product Owner.

## Onda 2 — Frente Fornecedor/CNPJ (ADR-0023, 12/08/2026)

Com a Onda 1 formalmente concluída (41/41, sem reabertura), o Product Owner formalizou a arquitetura de correção do fluxo de consulta/cadastro de Fornecedor por CNPJ (B2.2, já concluída) em **ADR-0023** (`.ai/DECISIONS.md`), a partir de `docs/audits/Discovery-Fornecedor-CNPJ-Linx-Compras.md` e `docs/audits/Arquitetura-Fornecedor-CNPJ-Decisao.md`. Decisões formalizadas: não persistir QSA; modelo híbrido de proveniência com snapshot bruto e retenção de 180 dias; sequência de Work Orders **B2.3** a **B2.9** (`.ai/BACKLOG.md`); Adapter Linx com planejamento autorizado e escrita real bloqueada até sessão de validação com especialista Visual Linx. A inconsistência documental de status interno da Work Order `B2.2` (arquivo dizia "Draft", governança já a tratava como Concluída) foi corrigida apenas no campo de status, sem reexecução.

**B2.3 — DocumentoFiscal: Normalização e Dígito Verificador — CONCLUÍDA** (12/08/2026, mesma sessão). `DocumentoFiscal` passa a ser o único Value Object canônico de documento fiscal do domínio +Compras: normaliza qualquer entrada (mascarada, com ruído alfanumérico ou já normalizada) para dígitos puros e valida o dígito verificador de CNPJ (14 dígitos) e CPF (11 dígitos) pelo algoritmo módulo 11 oficial, rejeitando sequências repetidas. `Cnpj.cs` deixou de duplicar a normalização — delega a `DocumentoFiscal.Create` e apenas restringe o resultado a CNPJ, eliminando a divergência que causava o BUG-4. `ConsultaCnpjResultado.CriarFalha` foi corrigido para não revalidar um documento já conhecido como inválido (evita reintroduzir o BUG-3: uma falha de formato não deve lançar exceção não tratada). Descoberta durante a implementação: um teste existente (`Import_Should_Allow_Cpf_And_Alphanumeric_Document`) documentava que a sincronização ERP aceitava documentos alfanuméricos (código legado Linx `CGC_CPF`) — comportamento que a ADR-0023 atribui exclusivamente a um futuro Adapter Linx; corrigido para que a sincronização de um documento alfanumérico falhe de forma controlada (`Status = "Falhou"`, sem persistir, sem exceção não tratada), coberto por teste dedicado. Nenhuma migration foi necessária (`dotnet ef migrations has-pending-model-changes` confirmou ausência de alteração de schema) — leitura de Fornecedores já persistidos não é afetada, pois a validação só ocorre em escrita. **Testes: 737 unitários + 7 integração aprovados, 0 falhas** (15 testes novos dedicados a `DocumentoFiscal`/`Cnpj`, fixtures de CNPJ/CPF sintéticos inválidos corrigidas para valores com dígito verificador real). Build limpo. Risco registrado, não resolvido nesta WO (escopo do B2.9/Adapter Linx): fornecedores Linx com `CGC_CPF` alfanumérico legado passam a falhar a sincronização ERP em vez de serem aceitos silenciosamente. Work Order movida de `active/` para `completed/`. **B2.4 a B2.9 permanecem `Draft`/Planejadas**, dependendo de nova autorização explícita do Product Owner. **B2.4 NÃO foi iniciada.**

## Auditoria de repositório

- **Etapa 1 — Higiene e artefatos gerados (30/07/2026):** remoção exclusiva de resíduos locais comprovados (`.DS_Store`, `bin/`, `obj/` e `dist/`) e reforço do `.gitignore`. Não houve alteração de progresso funcional. Restore serial, build e 231 testes foram concluídos; o único aviso `NU1900` decorre da indisponibilidade de consulta de vulnerabilidades ao nuget.org, sem impedir a validação. Ver [relatório da auditoria](../docs/audits/repository-cleanup-step-01.md).
- **Etapa 2 — Obsoletos, duplicados e órfãos (30/07/2026):** auditoria investigativa de 629 arquivos versionados. Foram registrados 13 grupos candidatos (0 para remoção automática); não houve alteração funcional, remoção, movimento ou renomeação. Ver [relatório da auditoria](../docs/audits/repository-cleanup-step-02.md).
- **Etapa 3 — Consolidação documental e estado A12 (30/07/2026):** fontes canônicas de visão e workflow definidas, documentos históricos controlados e saídas derivadas republicadas para refletir A12. Não houve alteração de progresso funcional. Ver [relatório da auditoria](../docs/audits/repository-cleanup-step-03.md).
