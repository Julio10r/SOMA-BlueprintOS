# Work Order — O1.5 — RBAC Real (Perfis, Permissões, Policies, Enforcement)

## Metadados

- Fase: Onda 1 — Fundação Funcional
- Sprint: O1.5 — sprint ativa desde 11/08/2026
- Status: **ATIVA — implementação concluída; Security Validation independente APROVADA COM RESSALVAS; aguardando aceite formal das ressalvas pelo Product Owner** (11/08/2026)
- Responsável: Claude Code (execução), Product Owner/CTO (aprovação)
- Prioridade: Caminho crítico — primeira frente do plano de conclusão da Onda 1
- Dependências: ADR-0020 (itens 7, 8, 9, 10); ADR-0021 (D1, D2)
- Data de abertura formal: 11/08/2026
- Data de conclusão: **pendente** — depende do aceite das ressalvas (mesmo precedente da O1.4.3)

## Objetivo

Implementar RBAC real com enforcement no backend (Perfil → Permissões → Policies/Authorization → endpoints protegidos), substituindo o mock atual de `administration/profiles`, em conformidade com a decisão D2 do Product Owner (ADR-0021).

## Contexto

Ver `docs/audits/Onda1-Reconciliacao-e-Plano-Execucao.md`, seção 5 (O1.5), e ADR-0020 (itens 7, 8, 9, 10) e ADR-0021 (D1, D2).

## Escopo

- Entregáveis oficiais cobertos (Onda 1, `DASHBOARD_STATE.md`): #9 "Perfis de usuário simulados", #17 "Perfis, papéis e permissões" (parcial de #11 "Módulo de Administração").
- Modelo de domínio `Perfil`/`Permissao`/`PerfilPermissao` com persistência real.
- Enforcement real via ASP.NET Core Authorization Policies sobre endpoints reais.
- Integração de `administration/profiles` à API real, removendo o mock.

## Fora de escopo

- Permissão individual ou exceção direta ao usuário (proibido por ADR-0020, itens 7/8/10).
- Microsoft Entra ID / IdP externo.
- CRUD de Usuários (coberto por O1.6).

## Arquitetura implementada

Fluxo completo do ciclo RBAC, do dado ao enforcement:

```
Perfil ──(PerfisPermissoes)── Permissao          [catálogo global, seed determinístico]
  │
  └──(UsuariosPerfis)── Usuario
                          │
                          ▼
        PermissoesEfetivasResolver  (Infrastructure — consulta EF Core)
          união dos Perfis ATIVOS vinculados, deduplicada
                          │
                          ▼
        ObterIdentidadeAtualUseCase  (Application — revalida sessão + permissões por requisição)
                          │
                          ▼
        SessionCookieAuthenticationHandler  (Api — publica claims `maiscompras_permissao`)
                          │
                          ▼
        HttpContext.User → PermissaoAuthorizationHandler → policy `permissao:<Codigo>`
                          │
                          ▼
        Endpoint protegido: 401 (sem sessão) / 403 (sem permissão) / 200 (autorizado)
```

Camadas seguem o mesmo padrão físico de O1.4.2/O1.4.3 (`Domain/Identity`, `Application/Identity`, `Infrastructure/Identity`, `Api/Authorization`, `Api/Administration`). Frontend: Vertical Slice `administration/profiles` já existente, convertida para consumir a API real.

**Fonte central única:** `Domain/Identity/PermissaoCatalogo.cs` declara os 14 códigos de permissão com Ids estáveis. Policies, seed de banco, endpoints protegidos e o catálogo devolvido ao frontend derivam todos dele — nenhum código de permissão é escrito literalmente em outro lugar (elimina o antipadrão de "strings mágicas duplicadas" vedado pela Work Order).

## Banco

Migration `20260811143355_AddRbacPerfilPermissaoCatalogo` (gerada por `dotnet ef migrations add`, com dois blocos de SQL acrescentados manualmente e documentados no próprio arquivo):

- `Perfis`: novas colunas `Descricao` (nvarchar(400), NOT NULL, default `''`), `CriadoEm` e `AtualizadoEm` (datetimeoffset, NOT NULL).
- `Permissoes`: `Descricao` estreitada de nvarchar(max) para nvarchar(400); **seed das 14 permissões** do catálogo, com Ids estáveis.
- `PerfisPermissoes`: FKs para `Perfis` e `Permissoes`, `ON DELETE RESTRICT`, + índice em `PermissaoId`.
- `UsuariosPerfis`: FKs para `Usuarios` e `Perfis`, `ON DELETE RESTRICT`, + índice em `PerfilId`.
- SQL manual (1): backfill de `CriadoEm`/`AtualizadoEm` das linhas de Perfil pré-existentes (evita o `0001-01-01` que o EF usaria como default).
- SQL manual (2): **backfill idempotente** concedendo o catálogo completo aos Perfis `Administrador Sênior` já existentes. Sem ele, o único administrador do ambiente ficaria com 403 permanente e o RBAC nasceria irrecuperável pela própria aplicação.

Índice único `IX_Perfis_UnidadeNegocioId_Nome` (de O1.4.3.1) **reaproveitado**, não recriado.

Migration aplicada ao banco de desenvolvimento `MaisCompras` em 11/08/2026 (necessária para o smoke test real exigido pela seção 12 das instruções). `has-pending-model-changes`: limpo. `migrations script 0`: exatamente 1 `CREATE TABLE [Fornecedores]` histórico, inalterado.

## Integrações

Nenhuma integração externa.

## Segurança e autorização

Security Validation **independente** executada em 11/08/2026 por revisor logicamente isolado (sem acesso ao raciocínio do implementador), cobrindo 14 classes de ataque/falha contra o código real — mesmo padrão da O1.4.3.4/O1.4.3. Veredito **APROVADA COM RESSALVAS**; 1 HIGH + 2 MEDIUM + 1 LOW corrigidos nesta sprint com teste de regressão, ressalvas remanescentes registradas em `.ai/BACKLOG.md`. Detalhe completo em `.ai/CURRENT_SPRINT.md`.

Decisões de segurança materializadas em código:

- Unidade de Negócio sempre da identidade autenticada, nunca do payload — isolamento entre BUs comprovado por teste.
- Códigos de permissão fora do catálogo são rejeitados (400), nunca ignorados.
- Claims de permissão emitidas exclusivamente pelo servidor; header forjado pelo cliente é ignorado (teste dedicado).
- Permissões reresolvidas a cada requisição — revogação tem efeito imediato, sem esperar expiração de sessão.
- Invariante anti-auto-bloqueio: nenhuma operação pode deixar a Unidade de Negócio sem Perfil ativo com `Perfil.Gerenciar` (409).
- Perfil inativo não contribui nenhuma permissão efetiva.
- Nenhuma tabela de permissão por usuário existe no modelo — teste de modelo falha se alguém introduzir uma.
- **Não-escalonamento de privilégio:** ninguém concede permissão que não possui (403). Fecha o achado HIGH da validação independente.
- **Invariante anti-auto-bloqueio exige usuário vinculado:** um Perfil administrativo sem ninguém vinculado não conta como salvaguarda.
- **Permissões efetivas escopadas à Unidade de Negócio da sessão**, impedindo escalonamento entre tenants.
- **CSRF no nível do grupo**, ciente de métodos seguros: endpoint novo nasce protegido.

## Testes

- Build: `dotnet build backend/BlueprintOS.sln` — 0 erros, 0 avisos. `npm run build` (`tsc -b && vite build`) — aprovado.
- Backend: **472 unitários + 5 integração = 477 aprovados, 0 falhas** (baseline da sessão: 393 + 5 = 398; +79 testes novos).
- Frontend: **61 aprovados, 0 falhas** (baseline: 53; +8 líquidos, com 11 testes novos de Perfis substituindo 3 do mock).
- Enforcement comprovado por teste automatizado de pipeline HTTP real (`RbacEnforcementPipelineTests`, host Kestrel real): 401 sem sessão, 403 autenticado sem permissão, 200 com permissão, composição de múltiplos Perfis, revogação imediata, header de permissão forjado ignorado, esquema de Development não autoriza.
- Smoke test real executado contra backend + frontend + SQL Server reais — ver relatório final.

## Documentação

- `docs/architecture/rbac-o1.5.md` (novo) — arquitetura, modelo de dados, fluxo de resolução, policies, endpoints, migrations, limitações.
- `docs/database/Database.md` — fatia administrativa de Perfis/Permissões (blueprint incremental, D7).
- `.ai/PROJECT_STATE.md`, `.ai/CURRENT_SPRINT.md`, `.ai/BACKLOG.md`, `.ai/dashboard/DASHBOARD_STATE.md`.

Nenhuma ADR nova criada: a mecânica RBAC já estava decidida pela ADR-0020 (itens 8/10) e a O1.5 a implementa sem decisão arquitetural nova.

## Critérios de aceite

- [x] Enforcement real comprovado por teste automatizado (não apenas schema) — `RbacEnforcementPipelineTests`, 12 cenários em host HTTP real.
- [x] Mock de Perfis removido e substituído por integração real — `perfisMockApi.ts` e o catálogo estático de permissões do frontend excluídos do repositório.
- [x] Build/testes verdes, sem regressão — 470 backend, 61 frontend, 0 falhas.
- [x] **Security Validation independente aprovada** — EXECUTADA por revisor logicamente isolado (11/08/2026). Veredito: **APROVADA COM RESSALVAS**; achados originais 0 CRITICAL / 1 HIGH / 3 MEDIUM / 4 LOW / 4 INFORMATIONAL. O HIGH (escalonamento de privilégio ilimitado por `Perfil.Gerenciar`), dois MEDIUM (escopo de BU nas permissões efetivas; invariante satisfeita por Perfil com zero usuários) e um LOW (CSRF opt-in) foram **corrigidos nesta sprint com teste de regressão**. Ressalvas remanescentes registradas em `.ai/BACKLOG.md` (O1.5-M1, O1.5-L1..L3, O1.5-I1..I4). **O aceite formal dessas ressalvas pelo Product Owner é o último passo para a conclusão** — ato humano, não realizado por esta sessão.
- [x] Documentação e blueprint atualizados.

## Riscos

Catálogo de **Perfis** (quais perfis existem e o que cada um contém) permanece pendência de conteúdo do Product Owner. A O1.5 entregou a mecânica e o catálogo de **Permissões** derivado exclusivamente dos códigos já documentados em `docs/product/ComprasFuncional.md` — nenhum perfil foi inventado. O único Perfil existente é "Administrador Sênior", criado pelo Bootstrap (O1.4.3.2).

## Decisões pendentes

1. Conteúdo do catálogo de **Perfis** (herdado de O1.4.1, seção 12) — não bloqueia a mecânica.
2. Nome definitivo da permissão de acesso do usuário a Centros de Custo (`CentroCusto.Acessar`?) — pendência já registrada em `ComprasFuncional.md`; deliberadamente **não** incluída no catálogo para não inventar nomenclatura.
3. Se a Security Validation independente é exigida para autorização (não apenas autenticação) — ver "Pendências".

## Pendências

- **Aceite formal das ressalvas pelo Product Owner** — único passo restante para a conclusão formal, exatamente como na O1.4.3. Ressalvas em `.ai/BACKLOG.md`: O1.5-M1 (invariante não serializada com a escrita), O1.5-L1 (backfill por nome de Perfil), O1.5-L2 (sem auditoria de RBAC), O1.5-L3 (sem rate limiting no grupo administrativo), O1.5-I1..I4.
- **D2 (ADR-0021) satisfeita apenas parcialmente:** `Fornecedor.*` e `Pedido.*` existem no catálogo, mas os endpoints de Fornecedores/Negociações seguem protegidos apenas por autenticação — fora do escopo declarado da O1.5.
- **Enforcement com sessão real de usuário sem permissão não pôde ser validado manualmente**: exigiria criar um segundo usuário, e a API de Usuários só existe na O1.6. O cenário está coberto por teste automatizado de pipeline HTTP real e pelo 403 real obtido via esquema de Development.
- Resíduo de smoke test no banco de desenvolvimento: dois Perfis inativos (`Analista (O1.5 smoke)`, `Aprovador (smoke UI)`). Não removidos porque o modelo não tem exclusão física, por decisão de produto.
- Enforcement RBAC aplicado aos endpoints de Fornecedores/Negociações **não** foi introduzido: está fora do escopo da O1.5 e seria mudança de comportamento em módulos que a Work Order não cobre.
- Divergência documental encontrada e corrigida em `PROJECT_STATE.md`: as migrations de Identity/Bootstrap constavam como "validadas mas não aplicadas" ao banco compartilhado; `dotnet ef migrations list` contra o banco real mostra **todas aplicadas**.

## Git Workflow

Conventional Commits (`WORKFLOW.md`). Commits criados localmente; **push não realizado** — pendente de autorização explícita do Product Owner.

## Relatório final

Ver relatório completo da sessão de execução (11/08/2026) e `.ai/CURRENT_SPRINT.md`.
